% ==========================================================================
% The weighted Goreinov--Tyrtyshnikov--Zamarashkin selection problem
% Build: make          (latexmk -> build/main.pdf)
% ==========================================================================
\documentclass[11pt]{amsart}

\usepackage[T1]{fontenc}
\usepackage{lmodern}
% A4.  The text block covers ~65% of the sheet, which is where the AMS
% journals sit; the folio clears the block by 14mm and the page edge by 11mm.
\usepackage[a4paper, left=24mm, right=24mm, top=22mm, bottom=25mm,
            footskip=14mm]{geometry}
\usepackage{amsmath,amssymb,mathtools,booktabs,array}
\usepackage{microtype}
\usepackage[dvipsnames]{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc,patterns}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage[most]{tcolorbox}
\definecolor{linkblue}{RGB}{0,102,187}
\usepackage[colorlinks=true,linkcolor=linkblue,citecolor=linkblue,
            urlcolor=linkblue]{hyperref}

% --- page furniture -------------------------------------------------------
% No running heads.  Folio centred at the foot, set one step larger than the
% body and dropped clear of the text block by the footskip above.
\makeatletter
\renewcommand{\ps@plain}{%
  \let\@mkboth\@gobbletwo
  \let\@oddhead\@empty
  \let\@evenhead\@empty
  \def\@oddfoot{\hfil\normalfont\large\thepage\hfil}%
  \let\@evenfoot\@oddfoot}
\makeatother
\pagestyle{plain}

% --- vertical rhythm ------------------------------------------------------
% Leading opened by 8% against the amsart default; the measure is narrowed to
% match.  Displays get a little more air than amsart gives them, which is
% what makes the longer computations readable.
\linespread{1.08}\selectfont
\setlength{\abovedisplayskip}{9pt plus 3pt minus 2pt}
\setlength{\belowdisplayskip}{9pt plus 3pt minus 2pt}
\setlength{\abovedisplayshortskip}{5pt plus 2pt}
\setlength{\belowdisplayshortskip}{6pt plus 2pt minus 1pt}
\setlength{\parskip}{0pt plus 1pt}

% --- draft layer (remove before submission) -------------------------------
\newtcolorbox{wip}{%
  enhanced, breakable, colback=white, colframe=black!55,
  boxrule=0pt, leftrule=1.2pt, arc=0pt, outer arc=0pt,
  left=8pt, right=2pt, top=3pt, bottom=3pt,
  fontupper=\small}

% ==========================================================================
% RESERVED NOTATION.  Every symbol below has exactly one meaning in this
% paper and is never rebound, not even inside a proof.  Symbols outside this
% list ($x$, $y$, $u$, $A$, $B$, $Y$, $Z$, ...) are proof-local bound
% variables, free to be reused across disjoint scopes; none of them shadows
% a reserved symbol.  Section 1 states the same table for the reader.
% ==========================================================================
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\FF}{\mathbb{F}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\tr}{\operatorname{tr}}
\newcommand{\diag}{\operatorname{diag}}
\newcommand{\rk}{\operatorname{rank}}
\newcommand{\lmin}{\lambda_{\min}}
\newcommand{\lmax}{\lambda_{\max}}
\newcommand{\psd}{\succeq}
\newcommand{\pd}{\succ}
\newcommand{\SC}{S_{C}}
\newcommand{\Gtz}{\mathrm{GTZ}}
\newcommand{\GtzC}{\mathrm{GTZ}_{\C}}
\newcommand{\Gram}{\operatorname{Gram}}
% transpose and adjoint, both set upright so that neither can be read as a
% variable.  Over R they agree; the paper writes whichever the field warrants.
\newcommand{\tp}{^{\mathsf{T}}}
\newcommand{\adj}{^{\mathsf{H}}}
% a design (calligraphic, as for every other structure in the paper)
\newcommand{\dsn}{\mathcal{D}}
% the frame operator of the dual family (Section 3)
\newcommand{\Sdual}{S^{\perp}}
% the tangent space to the design manifold (Section 8)
\newcommand{\tang}{\mathcal{T}}
% the critical value of the selection objective (Section 8)
\newcommand{\cv}{\nu}
% the Caratheodory window bound k(k+1)/2 + 1 (Section 5)
\newcommand{\win}{N}
% the graph carrying a graphic design (Section 6, Appendix B)
\newcommand{\gph}{\mathcal{G}}

\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{conjecture}[theorem]{Conjecture}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{example}[theorem]{Example}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\begin{document}

\title[The weighted GTZ selection problem]{The weighted
Goreinov--Tyrtyshnikov--Zamarashkin selection problem}

\author{Grigory Evko}
\email{evko.grigory@gmail.com}

\begin{abstract}
A weighted design of size $m$ and rank $k$ over $\FF\in\{\R,\C\}$ consists
of vectors $g_1,\dots,g_m\in\FF^{k}$ and positive weights $t_c$ summing to
one with $\sum_c t_c\,g_c^{\vphantom{\mathsf{H}}} g_c\adj=I_k$.  The selection
problem asks whether $k$ of the vectors dominate the identity outright,
$\sum_{c\in C} g_c^{\vphantom{\mathsf{H}}} g_c\adj\psd I_k$.  We solve the problem
over $\C$: for $k\ge2$ such a subset exists in every design precisely when
$m\le k+1$, and at $m=k+1$ the bound $1$ is attained at every weight
vector.  Over $\R$ we settle rank at most two and corank at most two,
reduce each rank to finitely many cells, and prove a variational
principle: granting the statement at the two cells one size below, if
selection fails at a cell then it fails at a global minimizer of the
selection objective over a compactification of the design space, and that
minimizer has strictly positive weights.  Rank three over $\R$ thereby reduces to the cells $(6,3)$ and
$(7,3)$, and at each cell to the exclusion of interior critical points
below the threshold.  The threshold is attained at every size, so no
argument for the conjecture can carry a uniform margin.  Statements whose head
carries the tag \emph{Lean} are verified in the Lean~4 proof assistant.
\end{abstract}

\maketitle
\thispagestyle{plain}
\tableofcontents

\input{sections/01-intro}
\input{sections/02-first}
\input{sections/03-chart}
\input{sections/04-complex}
\input{sections/05-real}
\input{sections/06-ties}
\input{sections/07-compactness}
\input{sections/08-interior}
\input{sections/09-formalization}
\input{sections/10-open}

\appendix
\input{sections/appendix-a}
\input{sections/appendix-b}

\bibliographystyle{amsplain}
\bibliography{refs}

\end{document}
