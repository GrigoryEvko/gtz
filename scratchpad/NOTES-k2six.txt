K2 AT (6,3) — THE FUNNEL ROUTE (fork: K2 -> design -> K1 lane, 2026-08-21)
==========================================================================

WHAT LANDED THIS ROUND (all pins standard, own audits, own gate-free per-module
builds; both umbrellas wired insertions-only)

  2397615  Gtz/Wave/KTwoSixThreeFunnel.lean
    funnel_dominator_meets_orthogonal_pair
    funnel_one_blind_pairMinor_zero
    k2SixThree_parallel_or_pairMinor_zero
  43d8b5e  Gtz/Wave/FunnelPairMinorHinge.lean   (12 theorems)
    pairGapMinor_of_leverage_eq_one, pairMinor_row_of_unitAtom,
    pairMinor_offRow_total_of_unitAtom, dominates_and_fixes_of_deflatedGapBound,
    isTie_sixThree_unitAtom_funnel_bounded, funnel_reading_sq_eq_pairMinor_div,
    funnel_parallel_of_two_pairMinors_zero, exists_outside_reading_sq_gt_one,
    one_lt_leverage_of_reading_sq_gt_one,
    pairGapMinor_lt_neg_one_of_reading_sq_gt_one, isTie_sixThree_funnel_structure
  19855d2  Gtz/Wave/BlindMemberPlane.lean
    nullProbe_in_plane_of_blind_member, funnel_blind_member_plane_and_pairMinor,
    k2SixThree_parallel_or_boundary_plane

K2-1. THE WEIGHT COUNT THAT KILLS THE AVOIDING CASE (kernel)
------------------------------------------------------------
At (6,3) with l_a = 1 and g_a orthogonal to g_y, g_z, a funnel dominator T
(avoids a, fixes g_a) CANNOT avoid both y and z.  If it did, T would be the
complement of {a,y,z}, and Parseval at g_a reads

    sum_{c in T} t_c m_c = 1 - t_a = t_y + t_z + sum_{c in T} t_c ,   m_c=(g_c.g_a)^2

while the funnel budget sum_{c in T} m_c = 1 plus t_c <= sum_T t_d caps the left
side at sum_T t_c.  Two positive weights have nowhere to go.  SIZE-BOUND ON
PURPOSE: it works because the complement of {a,y,z} at (6,3) is a triple.

K2-2. THE UNIT ATOM'S ROW OF THE PAIR-MINOR BUDGET (kernel, new law)
--------------------------------------------------------------------
l_a = 1  =>  pairGapMinor(g_a, g_b) = -(g_a.g_b)^2 for every b, hence

    sum_b t_a t_b pairGapMinor(g_a,g_b) = -t_a   (exact, every design, every size)

because Parseval at g_a totals the squared readings at l_a = 1.  Against the
landed pairMinor_budget (all rows total 1) the OTHER rows carry 1 + t_a.
A unit atom is a pure debtor and its debt is exactly its weight.

K2-3. SOME ATOM OUTSIDE THE FUNNEL DOMINATOR READS ABOVE ONE (kernel)
----------------------------------------------------------------------
The cap in K2-1 is STRICT (no single weight of a triple exhausts the triple's
weight sum, and some reading is positive), so the two atoms outside T carry
strictly more than their own weight, and one of them has (g_u.g_a)^2 > 1.
Consequences, both landed: l_u > 1 by Cauchy-Schwarz against the unit atom, and
pairGapMinor(g_a,g_u) < -1.  The landed predecessor of this needed the two-zero
stratum; this one needs only the funnel.

K2-4. HAND DERIVATION, NOT YET FORMALIZED — THE TWO-PLANE COLLAPSE
------------------------------------------------------------------
Configuration: (6,3) tie, no parallel pair, l_a = 1, g_a orthogonal to g_y,g_z,
funnel T = {y,d,d'} (by K2-1 exactly one of y,z is inside; take y).  Then

  (i)  m_y = 0, so m_d + m_{d'} = 1 and pairGapMinor(g_d,g_{d'}) = 0   [landed]
  (ii) g_a = m'_d g_d + m'_{d'} g_{d'}  with m'_c = g_c.g_a            [landed]
  (iii) by K2-3 the outside reader is e (NOT z, since m_z = 0), so m'_e =/= 0.

  Parseval as an operator at g_a (parseval_reproduces):
      t_a g_a + t_d m'_d g_d + t_{d'} m'_{d'} g_{d'} + t_e m'_e g_e = g_a
  substitute (ii) and collect:

      m'_d (t_a + t_d - 1) g_d + m'_{d'} (t_a + t_{d'} - 1) g_{d'}
        + t_e m'_e g_e = 0 .

  The last coefficient is nonzero, so {g_d, g_{d'}, g_e} IS LINEARLY DEPENDENT:
      tripleBracket(g_d, g_{d'}, g_e) = 0 .
  Hence FOUR atoms a, d, d', e lie in one plane P, and the whole component of
  Parseval orthogonal to P is carried by y and z alone:
      t_y (g_y.n)^2 + t_z (g_z.n)^2 = 1  for the unit normal n of P,
  with g_y, g_z in span(e2, n) because both are orthogonal to g_a in P.
  THE K2 (6,3) STRATUM COLLAPSES TO A TWO-PLANE CONFIGURATION.

  NOT YET A CONTRADICTION.  What is missing is a refusal argument against the
  two-plane configuration.  Handles available: EndpointSpike.
  tripleBracket_eq_zero_of_dependence turns the relation into the bracket
  statement in one call; the landed exists_basisTriple_avoiding_of_isTie_sixThree
  says every label of a (6,3) tie has a NON-degenerate triple avoiding it, which
  the two-plane configuration must still satisfy; and the campaign's measurement
  says K2 is empty at (6,3) with minimum obstruction 1.22e-2 over the ten
  avoiding triples.  Formalizing K2-4 needs the six labels enumerated, which is
  bookkeeping, not mathematics.

K2-5. WHAT THE FIXTURES SAY (guard against the graveyard law)
--------------------------------------------------------------
The (5,3) diamond tie has leverages (2, 13/4, 13/4, 13/4, 13/4) — NO unit atom,
so it does not inhabit the funnel branch and contradicts nothing above.  Its
eight weak dominators have PAIRWISE DISTINCT null directions (checked: the eight
unit null vectors are all different up to sign), which is exactly why the landed
mirror never fires there and why the diamond is a tie with no parallel pair.  A
funnel is the opposite regime: every weak dominator of the erased design fixes
the same unit atom, so all of them share one null.

RESIDUALS OF THIS LANE, UNCHANGED
----------------------------------
  * k2FiveTwoZero_kill still carries hB11 and hdet (the plane gap positive
    definite, written out in the produced probes).  Those are the stratum's own
    corank condition; discharging them from corank-one-ness is the remaining
    chart-to-design work.
  * K1 untouched this round: solve the active-set critical system (five of
    sixteen refusals active, six co-active at the Chebyshev optimum).  DO NOT
    DESCEND — three forks each "improved" the constant by an order of magnitude
    because the optimum is a Chebyshev point.


K2-6. K2-4 VERIFIED, FORMALIZED, AND THE REFUSAL MEASURED (two-plane lane)
==========================================================================
Commit 7f2addd  Gtz/Wave/TwoPlaneCollapse.lean  (8 theorems, all pins standard,
own audit, per-module build only, both umbrellas insertions-only)
  parseval_at_unitAtom_four_labels, unitAtom_plane_collapse_relation,
  twoPlane_bracket_eq_zero, unitAtom_plane_bracket_eq_zero, twoPlane_collapse,
  six_labels_cover, blindReading_cover, k2SixThree_parallel_or_two_plane

VERDICT ON K2-4: THE RECORDED DERIVATION IS CORRECT.  Every step checks, and two
of its stated consequences are not extra assumptions but automatic.

  * The collapse relation IS Parseval.  Rows (1,1) and (1,2) of Parseval, read in
    the plane, reproduce the relation exactly -- it carries no information beyond
    Parseval plus the plane relation.  So the derivation is one substitution.
  * q(g_d,g_d') = 0 is AUTOMATIC from the plane relation with |g_a| = 1.  In the
    parametrisation below it comes out identically, not as a constraint.
  * bracket(g_d,g_d',g_e) = 0 likewise.

EXACT PARAMETRISATION OF THE COLLAPSED FAMILY (basis e1,e2,e3; g_a = e1)
  g_d  = (cos th, -s sin th, 0)        g_d' = (sin th,  s cos th, 0)
      -- these give g_a = cos th * g_d + sin th * g_d' and q(g_d,g_d') = 0 identically
  g_e  = (p,q,0),  t_e p^2 = Lam := 1 - t_a - t_d cos^2 th - t_d' sin^2 th,
                   t_e p q = -s sin th cos th (t_d' - t_d)        [K2-3 needs p^2 > 1]
  g_y  = (0, gy, al),  g_z = (0, gz, be)
                   t_y al^2 + t_z be^2 = 1,   t_y gy al + t_z gz be = 0,
                   tau^2 = R t_y al^2 / t_z  with  R = 1 - (t_d s^2 sin^2 th
                            + t_d' s^2 cos^2 th + t_e q^2),  gz = tau,
                   gy = -t_z tau be/(t_y al)
  Eight free parameters plus three signs.  Parseval residual 1.0e-15 over 23522
  members; hypotheses reproduced: l_a-1 = 0, <g_y,g_a> = <g_z,g_a> = 0 exactly,
  q(g_d,g_d') = 7.6e-17, plane relation 1.4e-17, bracket(d,d',e) = 0.0,
  (g_e.g_a)^2 = 3.466 > 1, q(g_a,g_e) = -3.466 < -1.

THE REFUSAL IS TRUE BUT HAS NO UNIFORM MARGIN
  min over the family of  max_T lambda_min(S_T - 1):
      +0.256856  over 30351 samples (floor 0.05)
      +0.081073  under adversarial descent (floor 0.05)  -- sampling overstates by 3x
  ON THE ACTUAL BRANCH (adding S_{y,d,d'} >= M', which 4377 of 20268 members meet;
  note lambda_min(S_T - M') <= 0 ALWAYS because g_a is in its kernel, so the
  condition is an equality constraint, not an inequality -- a naive "max slack"
  test reads 0.000000 and proves nothing):
      floor 0.050 -> +0.132564      floor 0.020 -> +0.041245
      floor 0.010 -> +0.021109      (superset family: 0.105, 0.041, 0.020, 0.008
                                     at floors 0.05, 0.02, 0.01, 0.004)
  THE MARGIN IS ABOUT TWICE THE SMALLEST WEIGHT, on the branch and off it.  So no
  certificate with a positive constant can exist and any proof must consume
  weight positivity -- the same shape the cells lane reports for cell H.

THE DISJUNCTION IS IRREDUCIBLE -- FIVE TRIPLES, ALL THROUGH THE READER e
  Every maximiser contains e.  Per-triple adversarial infimum of
  lambda_min(S_T - 1) at floor 0.05, each of the five reaches -1.000000 alone.
  On the branch, floor 0.05, penalised infimum of the max over the set:
      all five {y,z,e},{y,d,e},{y,d',e},{z,d,e},{z,d',e}   +0.095595
      {y,z,e} alone                                        -1.000000
      {y,z,e},{y,d,e},{y,d',e}                             -0.209697
      {y,z,e},{z,d,e},{z,d',e}                             -1.000000
      the four without {y,z,e}                             -0.511017
  y and z are NOT interchangeable on the branch (y is the member inside the
  funnel dominator), which the third and fourth rows show.

TWO ROUTES TRIED AND CLOSED, DO NOT REDO
  * "No (5,3) tie has three coplanar atoms" is FALSE.  The diamond has two
    degenerate triples: incidence rows b0,b1,b3 and b0,b2,b4 each have zero
    determinant, and they are exactly its two non-dominating triples.  So
    coplanarity alone refuses nothing.
  * "The collapsed configuration forces |T| >= 4 dominating triples of the erased
    (5,3) design, contradicting the landed cap of 3."  Measured distribution of
    |T| over the family: 1:10830, 2:2365, 3:6156, 4:1531, 5:1269, 6:213, 7:13.
    Minimum 1, so no contradiction.  (The measurement is also weak evidence since
    family members are not ties.)

SCHUR REDUCTION THAT A SUCCESSOR SHOULD USE
  With P = span(e1,e2) and a Q-atom w = (0,w2,w3), for c1,c2 in P:
      {c1,c2,w} dominates  <=>  w3^2 > 1  and  M_{c1c2} >= diag(1, 1 + w2^2/(w3^2-1))
  as 2x2 matrices on P, where M_{c1c2} is the P-restriction of the two outer
  products.  For the funnel dominator this reads M_{d,d'} = diag(1, d2^2+d2'^2),
  so its e1 direction is EXACTLY tight -- that is the kernel ray ell g_a -- and
  the condition is d2^2+d2'^2 >= 1+kappa with kappa = gy^2/(al^2-1).  For the two
  candidates through e the (1,1) entry is d1^2+p^2 > 1 resp. d1'^2+p^2 > 1,
  strictly, because d1^2+d1'^2 = 1 and p^2 > 1.  The landed floor
  e2(T) >= (t_a/(1-t_a))^2 forces al^2 > 1 strictly.

RESIDUAL HYPOTHESES OF k2FiveTwoZero_kill (unchanged, checked)
  hunit, hy, hz            the stratum's own signature -- legitimate
  hRyzd, hRyze, hRyde, hRzde   the four refusals -- the tie's own content
  hne, hal, hax            outside atoms not parallel to the axis / live readings
  hB11, hdet               the plane gap positive definite (its corank condition)
  The last five are the chart-to-design residue at size five.  The new
  k2SixThree_parallel_or_two_plane carries NONE of them: it is unconditional at
  (6,3) and strictly stronger than k2SixThree_parallel_or_boundary_plane, which
  it supersedes (it adds the two brackets, the pair minor, the label
  distinctness and the reading bound 1 < (g_e.g_a)^2).


K2-7. CORRECTION TO K2-6 -- THE PENALISED BRANCH DESCENT IS UNRELIABLE
======================================================================
Caught by a direct check, and it would have entered the record as a structural
claim.  A penalised descent (objective = Phi + 200*max(0,-funnel_slack)) reported

    min over the branch of lambda_min(S_{z,d,d'} - I) = +0.000000

which reads as "the swap of the funnel dominator ALWAYS weakly dominates" -- a
second member of T, one swap from the first, sharing the null vector g_a.  It is
FALSE.  Direct sampling of 4366 branch members gives minimum -9.996e-01, with
1792 of them (41%) strictly negative.  The optimiser was trapped by the penalty
term and returned a boundary value.

WHAT IS STILL TRUE from that run, and worth keeping: S_{z,d,d'} DOES fix the unit
atom exactly (max |S g_a - g_a| = 2.2e-16 over all branch members), because
g_z is orthogonal to g_a and the plane relation supplies the rest.  So whenever
it does dominate it is a second element of T sharing the null vector -- but the
mirror is VACUOUS there, since both swapped readings (g_y.g_a) and (g_z.g_a) are
already zero, so mirror_readings_eq_zero_of_not_hasParallelPair gives nothing and
mirror_shared_pairGapMinor_eq_zero only re-derives q(g_d,g_d') = 0.

WHICH NUMBERS IN K2-6 SURVIVE
  RELIABLE (unpenalised, over the superset family; since branch is a subset of
  family, these bound the branch too):
      inf max_T lambda_min(S_T - 1) = +0.081073 at floor 0.05
      floor scaling 0.104507 / 0.040777 / 0.020301 / 0.008048
                    at floors 0.05 / 0.02 / 0.01 / 0.004      -- about 2*floor
      each of the five triples through e reaches -1.000000 on its own
  RELIABLE (sampling, an upper bound on the true minimum):
      4377 of 20268 members lie on the branch; min max_T there = +0.249097
  UNRELIABLE, DISREGARD: every figure in K2-6 produced with the penalty, namely
      the branch descent 0.132564 / 0.041245 / 0.021109 and the branch
      irreducibility table.  The family-level irreducibility test is unpenalised
      and stands.
  The refusal itself is unaffected: the family bound is unpenalised and positive.

A ROUTE TRIED AND CLOSED
  Whiten the three coplanar atoms.  Parseval gives their P-restricted weighted
  sum exactly diag(1-t_a, 1-rho) with rho = t_y gy^2 + t_z gz^2, so they form a
  weighted (3,2) design and GtzWeighted 3 2 (corank floor) hands back a
  dominating PAIR: M_{c1c2} >= diag((1-t_a)/tau, (1-rho)/tau), tau = t_d+t_d'+t_e.
  The first coordinate is free and STRICT, since tau = 1-t_a-t_y-t_z < 1-t_a.
  The second is not: the triple {c1,c2,w} needs M_{c1c2} >= diag(1, 1+kappa_w)
  with kappa_w = (g_w.e2)^2/((g_w.n)^2-1), and
      (1-rho)/tau - 1 >= min(kappa_y, kappa_z)
  FAILS at 1528 of 3319 branch members, worst slack -1.968245.  GtzWeighted 3 2
  gives the weakest pair, and the loss is entirely in the second coordinate.

THE SCHUR CRITERION IS THE RIGHT TOOL AND IS EXACT (unchanged from K2-6)
  For c1,c2 in the plane P = span(e1,e2) and w orthogonal to g_a,
      {c1,c2,w} dominates  <=>  (g_w.n)^2 > 1  and  M_{c1c2} >= diag(1,1+kappa_w).
  For the funnel dominator M_{d,d'} = diag(1, d2^2+d2'^2) exactly, so its e1
  direction is tight -- that is the kernel ray -- and domination reads
  d2^2+d2'^2 >= 1+kappa_y.  For the two candidates through the reader the (1,1)
  entry is d1^2+p^2 resp. d1'^2+p^2, STRICTLY above one, since d1^2+d1'^2 = 1
  and p^2 > 1.  A successor should work in these 2x2 terms.

K2-8. THE TWO-PLANE LANE, ROUND 3: THE REFUSAL IS NOT CLOSED, BUT THE
      CONFIGURATION IS PINNED TO FIVE TRIPLES, AND A NEW PRODUCER LANDED
      ON THE MAIN (6,3) FUNNEL BRANCH
==========================================================================

CALIBRATION.  Diamond reproduced exactly: leverages (2, 3.25 x4), pair minors
{0.75, 2.0, 4.5}, 8 of 10 dominating triples.

EXACT PARAMETRISATION (independent rederivation; agrees with K2-6 under
s <-> mu, th <-> theta, (p,q) <-> (X,Y), y,z <-> b,f).  Basis e1 = g_a, plane
P = span(e1,e2), e3 normal.  With u = cos th, v = sin th:
    g_a = (1,0,0)     g_d = (u,-mu v,0)     g_D = (v, mu u,0)
    g_e = (X,Y,0)     g_b = (0,pb,hb)       g_f = (0,pf,hf)
The plane relation, q(g_d,g_D) = 0 and bracket(g_d,g_D,g_e) = 0 hold
IDENTICALLY.  M_{dD} := g_d g_d^T + g_D g_D^T = diag(1, mu^2) exactly, so
g_d = A f_d, g_D = A f_D with A = diag(1,mu) and {f_d,f_D} ORTHONORMAL.
Tie heaviness of d and D forces mu^2 >= 1.
Eight free parameters (weights 5, th, mu, hb); Parseval residual 1.1e-15.
Parseval (1,3) is automatic, so only five of its six equations bite.
The off-plane pair satisfies W^T diag(t_b,t_f) W = diag(tau,1) on (e2,e3),
i.e. IT IS ITSELF A WEIGHTED RANK-TWO DESIGN, and inverting gives the three
exact relations  pb pf = -tau hb hf,  sigma = tau(1/t_b + 1/t_f - H),
D^2 = tau(K - H^2)  with sigma = pb^2+pf^2, D = pb hb + pf hf, H = hb^2+hf^2,
K = hb^2/t_b + hf^2/t_f.

*** ONLY FIVE OF THE TWENTY TRIPLES CAN STRICTLY DOMINATE ***
Measured over 78560 family members with every leverage >= 1: the triples that
are EVER strictly positive are exactly
    ebf (96.2%), Def (37.1%), def (36.6%), Deb (6.5%), deb (6.3%)
and the other fifteen never are.  The reason is structural and basis-free:
  (i)  l_a = 1 makes q(g_a,g_c) = -<g_a,g_c>^2 <= 0 for EVERY c, so by the
       landed pairGapMinor_nonneg_of_dominates the unit atom can only sit in a
       dominating triple beside atoms orthogonal to it -- and then the (1,1)
       gap entry is exactly 0, so such a triple is never STRICT.  (Landed this
       round the cheap way, as Gtz.unitAtom_notMem_posDef_triple, from
       Gtz.one_lt_leverage_of_posDef: the unit atom's leverage excess IS the
       first Sylvester minor.)
  (ii) The plane relation gives <g_d,g_a>^2 + <g_D,g_a>^2 = l_a = 1, and b,f
       are blind, so any triple inside {d,D,b,f} reads g_a at total <= 1 and
       cannot be strict (Gtz.reading_sq_sum_gt_of_posDef).
  (iii) {d,D,e} is coplanar, hence degenerate.
  Hence every strictly dominating triple contains the READER e and at least one
  off-plane atom.  Five triples remain.
  Numerically the three tight ones are dDb, dDf, abf, all at lambda_min exactly
  0 -- each covers the e1 direction exactly.

THE EXACT CRITERION FOR THE 96% TRIPLE.  {e,b,f} dominates iff H > 1 and
    (X^2 - 1) * SC >= Y^2,   SC := sigma - 1 - D^2/(H-1),
and SC is precisely the Schur complement of the (e2,e3) block of {b,f} against
the identity -- so SC >= 0 is exactly "{a,b,f} weakly dominates".  Since the
off-plane pair is a rank-two design, B >= (1/t_max) diag(tau,1) with
t_max = max(t_b,t_f), and Schur complements are operator monotone, giving the
clean bound  SC >= tau/t_max - 1  (verified, min gap +7.5e-07, and TIGHT when
t_b = t_f and hb = hf).  Hence the sufficient condition
    (X^2 - 1)(tau/t_max - 1) > Y^2       -- covers 92.23% of the family.

WHY THE REFUSAL DID NOT CLOSE.  {e,b,f} alone covers 96.2%.  In its failure
region def/Def/deb/Deb always work, and the (3,2)-design route covers 99.91% of
that region but NOT all of it (min over failures of rho - min(kappa) =
-0.082915, rho = (1-tau)/T1 - 1).  Imposing the funnel dominator WITH the
deflated bound (a genuine necessary condition I had omitted, and which cuts the
family to 75.8%) does not help: {e,b,f} still only covers 95.66% there, and the
sub-family minimum of max_T lambda_min is +0.1246.
Unpenalised descent, weight floor 0.05: inf max_T lambda_min = +0.0837,
reproducing K2-7's +0.081 independently.  Active set at the minimiser: def,
ebf, deb all at +0.0837, with dDb, dDf, abf all exactly 0.

*** WHAT LANDED INSTEAD, AND IT IS ON THE MAIN TARGET ***
Gtz/Wave/FunnelFourSetPayment.lean (commit 67d359b, 7 theorems, all pins
[propext, Classical.choice, Quot.sound], per-module build, both umbrellas
insertions-only).  NOT specific to the two-plane case -- it is a producer for
the whole FUNNEL branch of (6,3).
  The point: at a funnel, l_a = 1 makes g_a literally a UNIT vector, and the
  funnel dominator FIXES it, so g_a is a unit null probe of G = S_T - 1.  That
  is exactly the hypothesis the landed four-set law consumes.  Two closed forms:
      det(G + v v^T) = e2(G) * (v . g_a)^2                 [four-set law]
      e2 (G + v v^T) = e2(G) + leverageOf v * tr G - v.(G v)  [landed update]
  A four-set with det > e2 cannot have four nonpositive member floors
  (fourSet_det_le_secondInvariant_of_floors), and the member that IS the
  dominator pays det G = 0, so the positive member lies in the star of v, where
  star_exists_posDef_of_posDef_of_det_pos upgrades it to strict domination:
      *** Gtz.exists_star_posDef_of_payment_lt:  if
          leverageOf v * tr G - v.(G v)  <  e2(G) * ((v . g_a)^2 - 1)
          then one of the three swaps of a member of T for v STRICTLY dominates.
  Design level: Gtz.funnel_exists_strict_of_payment_lt, and the contrapositive
      *** Gtz.isTie_funnel_payment: a funnel boundary system pays
          e2 * ((g_p . g_a)^2 - 1)  <=  l_p * tr G - g_p.(G g_p)   at EVERY p,
  with Gtz.isTie_sixThree_funnel_payment supplying the dominator at six points.
  The right side is positive exactly where the landed
  exists_outside_reading_sq_gt_one produces a reading above one, so the law
  binds there and nowhere else.
  VERIFIED before proving: both update identities reproduce to 3.7e-13 over
  20000 random PSD forms with a unit null vector; the producer fired at 217 of
  20000 draws and delivered a strictly dominating triple every time, 0 failures.

WHAT A SUCCESSOR SHOULD DO.  The payment law is an exact scalar inequality on
every funnel boundary system.  Decompose g_p = r_p g_a + h with h perpendicular
to g_a; since G kills g_a and has eigenvalues 0 <= lam1 <= lam2 on g_a^perp,
    l_p tr G - g_p.(G g_p) <= lam2 l_p + lam1 r_p^2,   e2(G) = lam1 lam2,
so the producer fires whenever  lam1 lam2 (r_p^2 - 1) > lam2 l_p + lam1 r_p^2.
Bounding lam1, lam2 below at a funnel (the landed
pairMinorTotal_floor_of_deflatedGapBound gives lam1 lam2 >= (t_a/(1-t_a))^2) is
the remaining work.  That is a statement about the MAIN branch, not about the
two-plane configuration.
