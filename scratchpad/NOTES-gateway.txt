FORK 47 — G2, the corner's admissible outside pair (E1).  Self-contained.

STATUS: E1 NOT CLOSED.  Ten unconditional theorems landed (242c05a, 0dec1ab),
all [propext, Classical.choice, Quot.sound], module Gtz/Wave/CornerGatewayBudget.lean.

F47-1. THE COORDINATOR'S TWO CONJECTURES ARE BOTH TRUE — I DERIVED BOTH BY HAND.
  (a) adj(A) - adj(1-A) = (tr A - 1)I - A.  This is NOT a design law: it is a
      pure Cayley-Hamilton identity for EVERY real 3x3 matrix.  Proof: with
      adj(M) = M^2 - (tr M)M + e2(M)I and e2(I-A) = 3 - 2tr A + e2(A), the A^2
      terms cancel.  Verified 4.0e-15 on 2000 random (non-symmetric) matrices.
      LANDED as adjugate_sub_adjugate_one_sub.
  (b) tr(P_perp N_O P_perp) = 2 - tauC + s/lam.  Proof: tr N_O = 3 - (s+tauC),
      and the axis law gives u'N_C u = s(1+lam)/lam, so u'N_O u = 1 - s - s/lam.
      Verified 1.1e-14 on 2160 exact corners.  LANDED in scalar form as
      corner_planeTrace_surplus.

F47-2. THE NEW CENTRAL LAW — THE PAIR MINOR BUDGET OF A WEIGHTED TRIPLE.
  sum_{i<j} t_i t_j (1+q_ij) = (1-T)(S-1) + sum t_i^2 x_i
                               + t1t2t3 [g1g2g3]^2 + det(1 - sum t_i A_i)
  with x_i = l_i - 1, T = sum t_i, S = sum t_i x_i.  HYPOTHESIS-FREE: three
  vectors, three scalars, no Parseval, no design, no corner.  It is the landed
  det_one_sub_weighted_triple read in the pair minor instead of the wedge
  (w_ij = q_ij + l_i + l_j - 1); that shift is what turns a determinant formula
  into a budget.  Verified 3.1e-15 at the outside triple of 2160 corners AND at
  arbitrary triples of arbitrary designs (complement of any size).
  At a design: 1-T is the complement weight tauC, S-1 = 1-s, and the last term
  is det N_C >= 0 because the complement of a triple is a weighted sum of
  rank-one atoms (LANDED: complement_triple_posSemidef, det_complementTriple_nonneg,
  via parseval_triple_reading_le).

F47-3. THE CURRENCY BRIDGE (new, hypothesis-free, LANDED).
  [abc]^2 = tripleGapDet + (q12+q13+q23) + (x1+x2+x3) + 1.
  The characteristic polynomial of the Gram read at 1, every coefficient a
  campaign currency.  Refusals are written in tripleGapDet, admissibility in
  pairGapMinor, the hinge in the bracket; this converts between all three.

F47-4. THE REFINED BUDGET IN GAP CURRENCY (LANDED, exact).
  (weight slack) = sum (-q_ij) t_i t_j (1-t_k) + t1t2t3 tripleGapDet + det N_C
  where (weight slack) = e2(t) - (1-T)(S-1) - sum t_i^2 x_i - t1t2t3 (1 + sum x_i).
  The coefficient t_i t_j (1-t_k) is exactly what the crude budget discards.

F47-5. THE PRODUCERS AND THEIR MEASURED COVERAGE (each an unconditional theorem;
  the COVERAGE is a measurement, the DISJUNCTION IS NOT PROVED EXHAUSTIVE).
  On 181,249 exact random corners with all three outside atoms heavy:
    [F<0]   weight producer  (no bracket)      99.6546%
    [Fbr<0] bracket producer                   99.9691%
    [max(b_i+b_j)>1]  (parent's landed law)    99.9978%
    UNION of either producer with the b-law   100.0000%, zero misses
  Soundness: neither producer ever fired where all three q_ij <= 0.
  No corner sampled violated the gateway; min over corners of max outside q was
  +0.3687 (adversarial descent, 32 threads, 12-parameter exact corner chart).

F47-6. THE EXTREMAL, LOCATED EXACTLY.  Adversarial descent on the true 12-param
  corner manifold puts the minimiser at b_d -> 1/2 for ALL THREE outside atoms
  (measured b = (0.5087, 0.4997, 0.5013), sum 1.5097), tauC -> 0, lam -> 500+,
  s -> 1.  At the symmetric vertex b = (1/2,1/2,1/2) the weight slack is EXACTLY
  -tauC/2 (hand computation, confirmed): so the crude budget is negative there
  and the producer fires.  The residual sliver is tauC = 2(1-s) exactly, which
  is the same boundary the parent's case analysis reached from the other side.

F47-7. WHY THE SCALAR RELAXATION CANNOT PROVE EXHAUSTIVENESS — WITNESS.
  All three pairs inadmissible implies (i) b_i + b_j <= 1 (pairing cap) and
  (ii) F >= 0 (the budget).  That system is FEASIBLE: at
      t = (0.00035, 0.473, 0.497),  b = (0.96, 0.04, 0.04)
  every weight-level consequence holds and F = +0.215.  The point is killed only
  by the bracket term, worth 0.2245 there.  So any proof must carry the bracket.

F47-8. THE SIGN DICHOTOMY — the structure a successor should attack.
  Let sigma = sign(p12 p13 p23) (INVARIANT under atom sign flips, so genuine).
  Write q_ij = -r_ij, r >= 0.  Then exactly
      det(K_D - I) = -2 prod x + 2 sigma sqrt(prod (x_i x_j + r_ij)) - sum x_i r_jk .
  * sigma = +1:  sqrt >= prod x, hence det(K_D - I) >= -sum x_i r_jk, and
    substituting into F47-4 gives, using the member weight cap t_k l_k <= 1,
        F_plus := (weight slack of F47-4)  >=  sum r_ij t_i t_j (1 - t_k l_k) + det N_C  >= 0 .
    So [all q <= 0 AND sigma = +1] ==> F_plus >= 0.  CONTRAPOSITIVE IS A PRODUCER,
    and it KILLS the F47-7 witness (F_plus = 0.215 - 0.2256 = -0.0106 < 0).
  * sigma = -1:  the probe w = (s_i/sqrt(x_i)) with signs chosen so every cross
    term is negative gives 0 <= w'K_D w <= sum l_i/x_i - 6, i.e. sum 1/x_i >= 3;
    and det(K_D) >= 0 gives  sum_{i<j} r_ij l_k + 4 prod x_i <= 1 + sum x_i.
    NEITHER kills the F47-7 witness (sum 1/x = 47.4; 4 prod x = 19.6 << 2741).
  ON THE BOUNDARY r = 0 the Gram determinant is PINNED:
      det K_D = 1 + sum x_i + (2 sigma - 2) prod x_i ,
  i.e. 1 + sum x for sigma = +1 and 1 + sum x - 4 prod x for sigma = -1.
  At the witness BOTH values are ~2721-2741, and t1t2t3 times that is ~0.224 > F,
  forcing det N_C < 0 — so the witness dies in both branches once the bracket is
  carried exactly.  THE OBSTRUCTION IS PURELY THAT sigma = -1 HAS NO CLEAN
  LOWER BOUND ON det K_D AWAY FROM r = 0.  That is the one missing lemma.

F47-9. FIELD-BLINDNESS.  Per the coordinator, E1 holds at the complex corner tie
  (scratchpad/corner-ten/complextie.txt, q = +2.013/+6.116/+9.884).  Everything
  in F47-2..F47-8 is PSD/Parseval/ordering only — no chirotope, no bracket
  pinning, no realness — so the route is compatible.  The Hermitian Lagrange
  identity carries all the budgets to C verbatim.

F47-10. REFUTED, WITH WITNESSES.
  * "the pairing cap alone closes the deep regime" — FALSE.  Adversarial max of
    F over {t>0, sum t<1, b in fractional stable set polytope, sum a>1} is
    +0.2153, attained at the F47-7 witness.
  * "b_i + b_j <= 1 for all pairs forces sum b_d <= 3/2 hence a contradiction"
    — the implication is right but NOT a contradiction: the polytope max of
    sum A_d b_d is max(A_max, (sum A_d)/2), and since A_d < 1 the binding branch
    gives only tau_O <= 2s-1, i.e. tauC >= 2(1-s), which is the residual, not empty.
  * DO NOT quote a margin from sampling here: the gateway margin decays like
    (tauC + s/lam)/tau_O and tends to 0 along the extremal ray.  It is positive
    everywhere sampled but NOT bounded below.

TOOLING: scratchpad/f47/gw.jl (12-param exact corner chart, 32-thread descent).
Memory: all runs under 1 GB.

================== ROUND 2 (fork 47) — THE SIGN IS GONE ==================

F47-11. THE COORDINATOR'S sigma CASE SPLIT IS NOT NEEDED.  Landed 6e062dd,
Gtz/Wave/CornerRefusalDichotomy.lean, 8 theorems, all standard axioms.
  ISOLATION (hypothesis-free, one ring):
    tripleGapDet a b c = -2 x_a x_b x_c + (x_a q_bc + x_b q_ac + x_c q_ab)
                         + 2 (a.b)(a.c)(b.c)
  SQUARE (hypothesis-free, one ring) — THE POLYNOMIALISATION (angle 2):
    (tripleGapDet + 2 x_a x_b x_c - (x_a q_bc + x_b q_ac + x_c q_ab))^2
      = 4 (x_a x_b - q_ab)(x_a x_c - q_ac)(x_b x_c - q_bc)
  The Bargmann term is a ROOT OF A QUADRATIC, not an extra sign to case on.
  With all q <= 0 each factor is >= the product of its two excesses, so the
  square passes (2 prod x)^2 and the root lies outside the interval:
  DICHOTOMY (tripleGapDet_refusal_dichotomy) — NO SIGN ANYWHERE:
    (1)  x_a q_bc + x_b q_ac + x_c q_ab  <=  tripleGapDet        , or
    (2)  tripleGapDet <= -4 x_a x_b x_c + (x_a q_bc + x_b q_ac + x_c q_ab).
  This is field-blind: over C the same holds with Re(Delta3) in place of the
  product, whose square obeys the same bound.

F47-12. WHAT EACH BRANCH COSTS (both landed).
  BRANCH 1 PAYS THE BUDGET: fed into weightedTriple_pairMinor_gapForm, the
    coefficient against each pair minor is exactly the member weight slack
    1 - t_k l_k, so every term is nonnegative: branch 1 ==> refined budget >= 0.
    (branchOne_refinedBudget_nonneg)
  BRANCH 2 PAYS THE LEVERAGE: fed into the currency bridge with [abc]^2 >= 0,
    4 x_a x_b x_c <= 1 + sum x_i + (q_ab l_c + q_ac l_b + q_bc l_a), and with
    no admissible pair the last group is <= 0, giving the pure form
    4 x_a x_b x_c <= 1 + sum x_i.  (branchTwo_leverage_bound{,_pure})
  PRODUCER WITH A DISJOINT TRIGGER (exists_pairMinor_pos_of_refinedBudget_of_leverage):
    heavy triple + refined budget < 0 + 4 prod x > 1 + sum x  ==>  admissible pair.

F47-13. sigma AT THE COMPLEX TIE — angle 1, answered.  My Hermitian reading of
  scratchpad/corner-ten/complextie.pkl does NOT reproduce Parseval (residual
  0.77 in every convention I tried; the .txt says 1.55e-15, so the pkl stores a
  different normalisation).  DO NOT QUOTE MY COMPLEX NUMBERS.  What IS solid:
  the Bargmann invariant Delta3 = p12 p23 p31 is the field-blind object, its
  argument is the Pancharatnam phase, and sigma = +-1 only when that phase is
  0 or pi.  So sigma is a REAL-ONLY invariant — but F47-11 shows the proof does
  not need it, which is why the route stays field-blind.

F47-14. sigma = -1 IS THE COMMON BRANCH, NOT AN EDGE CASE.  Measured on 108523
  exact corners: sign(p12 p13 p23) = -1 at 80.04 percent.  The old framing would
  have closed only a fifth of the space.

F47-15. A WHOLE PRODUCER CLASS IS DEAD — REFUTED WITH A WITNESS.
  sum_{i<j} t_i t_j q_ij is NEGATIVE at 0.0405 percent of corners, minimum
  -0.176.  By the landed identity that quantity equals det N_O + det N_C - F
  exactly (checked to 7 digits).  CONSEQUENCE: no producer of the form "a fixed
  weighted sum of the three pair minors is positive" can EVER be total, for any
  choice of weights.  The gateway is irreducibly about the MAX, not a sum —
  the campaign's "select, never add" doctrine, now with a witness for E1.

F47-16. WHY BRANCH 2 STILL DOES NOT CLOSE, EXACTLY.  Chaining branch 2 with the
  pairing cap and AM-GM on det N_C (det <= (tr/3)^3) gives the necessary
  condition  F_plus <= PiA * S(b) + ((s+tauC)/3)^3  with
  S(b) = sum (1-b_i-b_j)(1-b_k) - 4 b1 b2 b3 and PiA = prod(1-t_i).
  Adversarial max of [F_plus - (PiA S + AMGM)] over the domain is -0.0373,
  attained at b = (1/2,1/2,1/2), s = 0.99918, tauC = 0.00205 — the same extremal
  as round 1.  WITHOUT the AM-GM term the margin is -8.8e-5, i.e. ESSENTIALLY
  ZERO: branch 2 forces det N_C to be nearly zero.  But at a corner
  det N_C = t_x t_y t_z (1 + lam) EXACTLY (Cauchy-Binet plus det S_C = 1+lam),
  which is strictly positive.  THE MISSING STEP IS A LOWER BOUND ON
  t_x t_y t_z (1+lam) IN TERMS OF THE OUTSIDE SCALARS.  That single inequality
  closes branch 2 and therefore E1.  It is the whole remaining gap.

============ ROUND 3 (fork 47) — BRANCH 1 IS UNIVERSALLY DEAD ============

F47-17. THE COORDINATOR'S "NEW" IDENTITY IS MY LANDED ONE, REARRANGED.
  det N_C = (tauC + s - 2) + e2(N_O) - det N_O   is TRUE (char poly of N_O at 1
  plus tr N_O = 3 - tauC - s), but expanding e2(N_O) = sum t_i t_j w_ij and
  w_ij = q_ij + x_i + x_j + 1 turns it into
     det N_C = (1-T)(1-S) + sum t_i t_j q_ij - sum t_i^2 x_i + e2(t) - det N_O ,
  which is weightedTriple_pairMinor_identity verbatim.  I verified the algebra.
  So it adds NO information beyond what round 2 already used, and it does not
  replace the AM-GM step: the AM-GM was an *upper* bound on det N_C layered ON
  TOP of the identity, and branch 2 produces a *lower* bound on det N_C, so the
  two never meet.  Direction, not sharpness, is why branch 2 survived.

F47-18. THE EXACT CANCELLATION AT THE EXTREMAL (sympy, exact).
  At b = (1/2,1/2,1/2) and ANY weights:
     F  = -tauC/2 ,        F_plus - PiA*S(1/2) = 0   IDENTICALLY,
     4*prod x - (1 + sum x) = tauC / (2*prod t)  > 0  STRICTLY.
  So at the relaxation's extremal BOTH branches die: branch 2 needs
  4 prod x <= 1 + sum x, which fails by exactly tauC/(2 prod t); and branch 1
  needs F_plus >= 0, which fails by exactly PiA/2.  The landed producer
  exists_pairMinor_pos_of_refinedBudget_of_leverage fires there.
  This is why every descent kept landing on b = 1/2 with margin ~1e-4: that
  point is an EXACT double root of the relaxation, not a numerical artifact.

F47-19. BRANCH 1 IS UNIVERSALLY IMPOSSIBLE — the round's main advance.
  Adversarial scan (800k random + 40x40k polish) over the whole domain
  {t_i>0, T<1, b_i>0, b_i+b_j<=1, sum b_i A_i > 1}:
      sup F_plus = -0.00118587  < 0
  attained at the degenerate edge t = (0.948, 3.1e-4, 1.97e-3), b sum 1.0229.
  Since branchOne_refinedBudget_nonneg (LANDED) says branch 1 ==> F_plus >= 0,
  this says: A REFUSING HEAVY TRIPLE AT A CORNER IS ALWAYS IN BRANCH 2.
  [MEASURED, not proved.  It is a scalar polynomial inequality on a bilinear
  domain, so it is a Handelman/Positivstellensatz target in the (t,a) variables
  — that is the single cheapest remaining brick.]

F47-20. WHAT IS LEFT, EXACTLY.  With branch 1 dead, all q <= 0 forces
     4 x_1 x_2 x_3  <=  1 + x_1 + x_2 + x_3        (branchTwo_leverage_bound_pure)
  and that is NOT contradictory by itself: sup over the domain of
  [1 + sum x - 4 prod x] is +3.46e5, so branch 2 is available on a large region.
  Chaining branch 2 with the pairing cap and AM-GM gives only
  F_plus - PiA*S(b) <= det N_C, a LOWER bound (max of LHS = -8.8e-5 <= 0),
  so it never conflicts with det N_C = t_x t_y t_z (1+lam) > 0.
  THE REMAINING GAP IS THEREFORE: exclude branch 2 on the region where
  4 prod x <= 1 + sum x.  Everything else in E1 is closed.

============ ROUND 4 (fork 54) — E1 REDUCES TO A MATRIX STATEMENT ============

F54-1. THE REFINED BUDGET IS A PRODUCT (landed, Gtz.refinedBudget_factor).
  With A_i = 1 - t_i the weight slack and a_i = t_i x_i the weighted excess,
      F  =  A1 A2 A3  -  a1 A2 A3  -  a2 A1 A3  -  a3 A1 A2
         =  A1 A2 A3 * (1 - b1 - b2 - b3) ,     b_i = a_i / A_i .
  ONE ring.  Every earlier estimate of F was an estimate of a PRODUCT, which is
  why none were sharp, and why the "sup F = -0.00118587" of F47-19 is wrong as a
  supremum: the true sup is 0, approached as sum b -> 1+.  The scan simply could
  not reach a boundary it does not contain.  No Handelman was needed.

F54-2. BRANCH ONE IS UNIVERSALLY IMPOSSIBLE — PROVED, ONE LINE
  (Gtz.refinedBudget_neg_of_excess, Gtz.outsideTriple_not_branchOne).
  A slack is < 1, so b_i > a_i, so sum b > sum a.  A corner gives its outside
  triple sum a > 1 (total excess 2, inside share < 1), hence sum b > 1, hence
  F < 0.  Branch one forces F >= 0.  Done.  F47-19's measured claim is now a
  theorem, and the producer loses its budget hypothesis
  (Gtz.exists_pairMinor_pos_of_excess).

F54-3. THE WHOLE DICHOTOMY IS TWO DETERMINANTS OF ONE MATRIX.
  Let M be the WEIGHTED outside Gram, M = V_O^T V_O with V_O V_O^T = N_O.  M has
  the same spectrum as N_O = I - N_C, so
      0 <= M <= I        (M >= 0 since N_O >= 0 ; M <= I since N_C >= 0).
  Put D = diag(t), K = M - D.  Then K = D^{1/2}(G_O - I)D^{1/2}, so
      K_ii = a_i ,   K_ij = sqrt(t_i t_j) p_ij ,   det K = t1t2t3 * tripleGapDet.
  INADMISSIBILITY q_ij <= 0  IS EXACTLY  a_i a_j <= K_ij^2 , i.e. every 2x2
  PRINCIPAL MINOR OF K IS <= 0.  And with sigma = sign(M12 M13 M23):
    * det(I - M) >= 0 , sigma >= 0  ==>  A1A2A3 * (1 - sum b) >= 0  ==>  sum b <= 1
      -- because  Prod(1-b) - Sum(1-b_k) b_i b_j - 2 b1b2b3  =  1 - sum b  EXACTLY
         (all quadratic and cubic terms cancel; sympy-verified).  THIS IS F54-1.
    * det M >= 0 , sigma < 0     ==>  4 a1a2a3 <= t1t2t3 + Sum a_i t_j t_k ,
      i.e. 4 prod x <= 1 + sum x  -- EXACTLY branch two (sympy-verified).
  So branch one IS the sigma >= 0 half read through det(I-M), branch two IS the
  sigma < 0 half read through det M, and the Bargmann term of F47-11 is
  M12 M13 M23.  The squaring trick and this reading agree.

F54-4. E1 REDUCES TO A PURE MATRIX STATEMENT — NO CORNER GEOMETRY.
  CONJECTURE (measured, sup attained at 1):
     M symmetric 3x3 with 0 <= M <= I ;  t_i > 0 ;  a_i = M_ii - t_i > 0 ;
     every 2x2 principal minor of K = M - diag(t) is <= 0
       ==>   a1 + a2 + a3  <=  1 .
  A corner's outside triple has sum a > 1, so E1 follows immediately.
  MEASURED: maximising sum a over the all-inadmissible set (the maximiser has all
  three pair constraints TIGHT, so a_i = |P|/M_jk^2 with P = M12M13M23):
      global, 400 restarts    sup sum a = 0.999999999009    (never exceeds 1)
      sigma > 0 branch        sup sum a = 0.99985613
      sigma < 0 branch        sup sum a = 0.99466060
  The supremum is 1, approached as spec(M) -> [0, eps, 1] with sum t -> 0.
  ALSO: 1.5M random draws, 267831 heavy-with-excess, EACH pair inadmissible about
  3.1 percent of the time, ALL THREE never (0 of 267831).
  STATUS: the sigma >= 0 half IS PROVED (F54-2).  The sigma < 0 half is MEASURED
  ONLY.  Do not quote it as a theorem.

F54-5. WHY THE EARLIER SCALAR RELAXATIONS FAILED — REFUTED WITH WITNESSES.
  (a) {b_i + b_j <= 1, sum a > 1, sum t < 1, branch 2} IS FEASIBLE:
      min of 4 prod x - 1 - sum x reaches -6.10e3 at
      t = (0.2973, 1.62e-4, 0.5470), b = (0.0081, 0.9900, 0.0099).
      So the pairing cap alone can never exclude branch two.
  (b) Adding only M <= I (i.e. Kcheck <= I) still leaves branch two feasible:
      36 points found, min G = -943.6.
  (c) Adding M >= 0 as well kills it: 0 points in 150423 draws.
      THE MISSING CONSTRAINT WAS N_O >= 0, NOT MORE CORNER GEOMETRY.
  Consequence: any future relaxation of E1 must carry BOTH determinant
  conditions of M.  Pairwise data is provably insufficient.

F54-6. WHAT IS LEFT.  Prove F54-4 in the sigma < 0 branch.  Structure known:
    * sigma < 0 itself forces  t1t2t3 + Sum t_i t_j a_k  >  2 a1a2a3  (from
      det M >= 0 with |P| >= prod a), and branch two sharpens 2 to 4.
    * M <= I gives the diagonal bound a_i <= A_i (b_i <= 1) and pairwise
      b_i + b_j <= 1, hence with b1 <= b2 <= b3 : b1 <= b2 <= 1/2.
    * Those alone give only sum a <= 1 + b1 <= 3/2.  The remaining content is
      that b1 (the SMALLEST normalized excess) is forced to zero as sum a -> 1,
      which is what the two determinant conditions do jointly.
  It is a scalar polynomial system in (a, t, M12, M13, M23) on a semialgebraic
  domain -- a Positivstellensatz target, and the right one, unlike the earlier
  bilinear (t,a) target of F47-19 which is now a proved theorem instead.

F54-7. THE sigma < 0 BRANCH, REDUCED TO AN EXACT SCALAR SYSTEM.
  At the maximiser all three pair constraints are tight, so K = W E W with
  W = diag(sqrt a) and E the sign matrix.  Up to the diagonal congruence
  w_i -> -w_i (which leaves sigma invariant) there are exactly two patterns:
    sigma = +1 :  E = all-ones, RANK ONE, K = v v^T.  Then M <= I is the
      rank-one condition sum a_i / A_i <= 1, i.e. SUM b <= 1 -- F54-1 again,
      and PROVED.
    sigma = -1 :  E = [[1,1,1],[1,1,-1],[1,-1,1]], eigenvalues {-1, 2, 2}.
      Sympy-exact:  det(I - U E U) = 1 - sum b + 4 b1 b2 b3 , so
         det(I - M) >= 0   <=>   SUM b <= 1 + 4 b1 b2 b3 .
  So the whole sigma < 0 branch is the scalar implication
      b_i in [0,1],  b_i + b_j <= 1,
      sum b <= 1 + 4 b1 b2 b3,                     [det(I-M) >= 0]
      4 prod x <= 1 + sum x ,  x_i = b_i (1-t_i)/t_i,   [det M >= 0]
      t_i > 0,  sum t < 1
        ==>   sum a = sum b_i (1 - t_i)  <=  1 .
  MEASURED on this reduced system: max sum a = 0.999999999, never exceeding 1,
  the supremum approached at b -> (0,0,1) with sum t -> 0.
  SIX variables, all constraints polynomial, domain semialgebraic and the bound
  TIGHT (margin 0) -- so a Positivstellensatz certificate exists but cannot be
  strict.  THIS is the target, and it is far smaller than anything the lane has
  attacked before.
  USEFUL SUB-FACT (proved by hand, one line): branch two forces min_i x_i <= 1.
    If every x_i > 1 write x_i = 1 + e_i ; then 4 prod x - (1 + sum x) =
    3 sum e + 4 sum e_i e_j + 4 prod e > 0, contradiction.  So some a_k <= t_k.

============ ROUND 5 (fork 58) — THE TWO DETERMINANTS ADD, AND THE REAL GAP ============

F58-1. THE SUM OF THE TWO DETERMINANT CONDITIONS IS LINEAR (LANDED, one ring,
  Gtz.branchTwo_determinant_sum in Gtz/Wave/BranchTwoDeterminantSum.lean).
  With A_i = 1-t_i and a_i the weighted excess:
     detIM = A1A2A3 - sum a_i A_j A_k + 4 a1a2a3          (= det(I-M))
     detM  = t1t2t3 + sum a_i t_j t_k   - 4 a1a2a3        (= det M)
     detIM + detM = (A1A2A3 + t1t2t3) - sum_i a_i (1 - t_j - t_k) .
  The excess product CANCELS EXACTLY and the coefficient of a_i collapses:
     A_j A_k - t_j t_k = 1 - t_j - t_k   (Gtz.branchTwo_coefficient_eq).
  Hence both conditions together give ONE LINEAR inequality in the excesses
  (Gtz.branchTwo_excess_linear_bound), with NO case on the Bargmann sign.

F58-2. THE LINEAR BOUND IS TOO WEAK — REFUTED WITH A WITNESS (scratchpad/f58/lin.py).
  Maximising sum a against ONLY the linear bound + pair constraints is an LP.
  Its optimum is 1.0671454587686742 at
     t = (0.343434507, 0.377245643, 1.01772868e-4),
     a = (0.15801287, 0.14987568, 0.75925691).
  Dropping the cancelled cubic loses exactly the part that makes the bound tight.
  THE TWO DETERMINANT CONDITIONS MUST BE SPENT SEPARATELY.  Do not sum them.

F58-3. NO SINGLE WEIGHTED COMBINATION WORKS EITHER (scratchpad/f58/lam.py).
  Maximising sum a subject to detIM + lam*detM >= 0 (plus pair, domain):
     lam    = 0.25   0.5    1.0    2.0    4.0
     max    = 1.1698 1.0741 1.0354 1.0240 1.0360
  ALL exceed 1; the best (lam ~ 2) is still 1.024.  The one-parameter family of
  combinations is dead.  Both conditions are needed AT FULL STRENGTH.

F58-4. CORRECTION TO F54-4/F54-7: THE MAXIMISER DOES **NOT** HAVE ALL THREE PAIR
  CONSTRAINTS TIGHT.  All three tight means b_i+b_j = 1 for every pair, which
  forces b = (1/2,1/2,1/2) exactly (Gtz.pairTight_forces_center) -- the centre.
  At the centre:
     detIM = 0 exactly                  (Gtz.centerPoint_detComplement_zero)
     sum a = (3 - sum t)/2 > 1          (Gtz.centerPoint_excess_gt_one)
  so the TARGET FAILS there on the pair constraints and detIM alone.  What rules
  the centre out is detM, whose value there is exactly
     detM|center = (t1+t2+t3-1)/2       (Gtz.centerPoint_detOutside_eq)
  which is NEGATIVE on the stratum (Gtz.centerPoint_detOutside_neg).
  So detM is not decoration: it is THE ONLY THING standing at the centre, and it
  is what fork 47's "exact double root at b=(1/2,1/2,1/2)" was detecting.
  MEASURED (scratchpad/f58/desc.py, adversarial descent, and at.py, random):
     descent max sum a = 0.9999976465  at b = (0.0317, ~0, 0.9683), NO pair tight
     random  max sum a = 0.9971413705  at b = (0.362, 0.172, 0.619), NO pair tight
  Supremum is 1, never exceeded, approached with t -> 0 and one b_i -> 0.

F58-5. THE WHOLE OF F54-7 REDUCES TO ONE LEMMA.  Since
     sum a = sum b_i (1-t_i) = sum b - sum b_i t_i      (exact identity)
  and detIM >= 0 IS  sum b <= 1 + 4 b1b2b3, the target sum a <= 1 follows from
     KEY:   4 b1 b2 b3  <=  b1 t1 + b2 t2 + b3 t3
  because then  sum a <= 1 + 4 prod b - sum b_i t_i <= 1.  One line.
  KEY IS MEASURED TRUE AND TIGHT ONLY AT b=0:
     adversarial descent (600 restarts x 15000 steps, scratchpad/f58/key_test.py):
         MAX of [4 prod b - sum b_i t_i] = 0.0 EXACTLY, attained only at b = 0.
     nontrivial points b_i >= 0.05 (76037 feasible, scratchpad/f58/key2.py):
         MAX = -0.0014057727567717733  -- strictly negative throughout.
  STATUS: MEASURED ONLY.  Do not quote KEY as a theorem.

F58-6. WHY KEY HAS NO HANDELMAN CERTIFICATE YET, AND WHAT IS MISSING.
  Handelman LP over {b_i, t_i, A_i, S=1-sum t, pair, detIM, detM}:
     degree 2: 136 products, 201 monomials -- INFEASIBLE
     degree 3: 816 products, 1075 monomials -- INFEASIBLE
  (For the ORIGINAL target sum a <= 1 the same LP is infeasible at degree 3 and
   degree 4 exceeds the time budget.)  The obstruction is structural, not degree:
  KEY is TIGHT AT b = 0 where BOTH SIDES VANISH, so no certificate with a
  strictly positive constant term can exist -- exactly the "cannot be strict"
  warning of F54-7, now located precisely.
  WHAT THE PAIR CONSTRAINTS GIVE ALONE (proved by hand, one line):
     b_i + b_j <= 1 forces b2*b3 <= ((b2+b3)/2)^2 <= 1/4, so 4 b1b2b3 <= b1,
     hence KEY >= b1(t1 - 1) + b2 t2 + b3 t3, which is NOT sign-definite.
  So the pair constraints alone are provably insufficient and detM must supply
  the remainder.  A successor should either (a) find the Positivstellensatz
  certificate for KEY with the b=0 degeneracy factored out -- e.g. certify
  KEY/(b1+b2+b3) or KEY on the slice sum b = 1 -- or (b) prove KEY by a direct
  case split on which b_i is largest, using detM only in the branch where the
  pair bound fails.

F58-7. WHAT IS LANDED THIS ROUND (Gtz/Wave/BranchTwoDeterminantSum.lean, commit
  4f05761, 9 theorems, all pins [propext, Classical.choice, Quot.sound]):
     branchTwo_determinant_sum        the cancellation identity
     branchTwo_excess_linear_bound    the linear consequence
     branchTwo_coefficient_eq         A_jA_k - t_jt_k = 1 - t_j - t_k
     pairTight_forces_center          all three pair tight => b = (1/2,1/2,1/2)
     centerPoint_detComplement_zero   detIM = 0 at the centre
     centerPoint_excess_sum           sum a = (3 - sum t)/2 at the centre
     centerPoint_excess_gt_one        so the target fails there
     centerPoint_detOutside_eq        detM|center = (sum t - 1)/2
     centerPoint_detOutside_neg       hence detM < 0 on the stratum
  E1 IS NOT CLOSED.  The remaining gap is KEY of F58-5, and it is one scalar
  inequality in six variables with the degeneracy located.

F58-8. ITEM 0 — THE 3-vs-1 ASYMMETRY IS A THEOREM, NOT A MEASUREMENT
  (LANDED, Gtz/Wave/CornerDescentRank.lean, commit 5f8c59c, 5 theorems, all
   pins [propext, Classical.choice, Quot.sound]).
  The coordinator relayed a measured rank-3 result over 297 corners.  It is
  exact, and the proof is one line of the corner equation.
  Let S_C - 1 = lam u u^T with |u|=1, and let B carry an orthonormal basis of
  the kernel plane u^perp.  Because B is orthogonal to the axis, the axis term
  DIES under the congruence:
      sum_{c in C} (B^T g_c)(B^T g_c)^T  =  B^T S_C B  =  I_2 .
  THE PROJECTED INSIDE ATOMS ARE A PARSEVAL FRAME FOR THE KERNEL PLANE, at every
  corner, with no genericity.  Landed at probe level as
      corner_kernel_probe_form         sum_c (g_c . v)^2 = v.v      (u.v = 0)
      corner_projectedAtoms_resolve    sum_c (g_c.v)(g_c.w) = v.w   (u.v=u.w=0)
      corner_projectedAtom_ne_zero_of_probe   no kernel probe is missed
      corner_kernel_reading_total / _cross    the orthonormal specialisations
  CONSEQUENCE: the reachable set of gap perturbations on the kernel contains
  w h^T + h w^T for every projected atom h and every w, and those span Sym(2)
  as soon as the h span the plane -- which the resolution guarantees.  So a
  CORNER must block a 3-DIMENSIONAL descent where a corank-one tie blocks 1.
  MY OWN MEASUREMENT BEFORE PROVING (scratchpad/f58/probe0.py): 400 random real
  corank-two corners, rank 3 in ALL 400, third singular value EXACTLY
  1.4142135623730934 (= sqrt 2, constant, median 1.4142135623730951).  A
  CONSTANT third singular value is the signature of a resolution of the
  identity; a generic rank count would not produce one.  That constant is what
  told me it was an identity rather than a measurement.

F58-9. THE 3-vs-1 STRUCTURE DOES NOT, BY ITSELF, DELIVER E1 — honest scope.
  The descent statement is about which perturbations of the INSIDE atoms a
  corner must block.  E1 is a statement about the OUTSIDE pairs' minors.  The
  resolution constrains the inside geometry completely and says nothing directly
  about q_dd'.  So it does NOT replace the six-variable scalar fight; it is a
  strictly stronger structural fact about corners that a successor should use
  when attacking corner EMPTINESS, not E1.
  E1 itself re-confirmed independently this round (scratchpad/f58/e1try.py):
  14495 freshly built corank-two corners, some outside pair admissible in
  14495 of them -- 100.0000%, zero exceptions.

F58-10. WHAT A SUCCESSOR SHOULD DO WITH KEY (F58-5), in priority order.
  (a) Certify KEY with the b=0 degeneracy factored out.  KEY vanishes to first
      order at b=0 in every variable, so certify KEY on the slice sum b = 1, or
      certify KEY/(b1+b2+b3), or homogenise.  The Handelman LP over
      {b_i, t_i, A_i, 1-sum t, pair, detIM, detM} is INFEASIBLE at degree 2 (136
      products, 201 monomials) and degree 3 (816 products, 1075 monomials);
      degree 4 exceeds the time budget on the original target.
  (b) Case split on which b_i is the largest, using detM only where the pair
      bound fails.  The pair bound alone gives 4 b1b2b3 <= b1 (from b2b3 <= 1/4),
      which suffices iff 4 b_j b_k <= t_i for i = argmax b.  MEASURED: that
      sufficient condition FAILS at 32439 of 96009 feasible points (a third of
      them), worst case 4 b_j b_k - t_i = +0.8548978120895784 at
        b = (0.45632135, 0.484212, 0.51006457), t = (0.84108709, 0.11937363, 0.0289273)
      where KEY is nonetheless +0.005555059093889092.  So the split is real but
      the hard branch needs detM, exactly as the LP infeasibility predicts.
      In the hard region (32511 points) min KEY = +2.1889923769500106e-06 at
        b = (1.07566628e-05, 7.78980257e-03, 6.85224350e-01)
        t = (1.06059930e-01, 1.54488960e-04, 1.08532366e-07)
      -- tight but never violated.
  (c) The Cayley rational corner chart (relayed by the coordinator this round,
      Q = (I-K)(I+K)^{-1} for skew K, inside atoms the columns of
      diag(1,1,mu) Q with mu = 1+e^s) is polytope-compatible and may make the
      domain a polytope where the (b,t) variables do not.  UNTESTED BY ME.

F58-11. THE CASE SPLIT OF ROUTE (b), MAPPED — THE CORE IS SMALL AND NOT TIGHT.
  Write i = argmax b, and o = the other two indices.
  STEP 1 (pure pair constraints, proved by hand): b_j + b_k <= 1 gives
     b_j b_k <= ((b_j+b_k)/2)^2 <= 1/4,  so  4 b1b2b3 <= b_i .
  STEP 2 (measured decomposition of the remaining work, 3e6 draws):
     * 4 b1b2b3 <= sum_{x in o} b_x t_x  -- i.e. THE OTHER TWO TERMS ALONE carry
       KEY -- at 97.3 percent of the hard branch (31255 of 32132).
     * The CORE where b_i t_i is genuinely required is 21140 points of 3e6 draws.
       min KEY on the core = +0.0005337875696401677  -- BOUNDED AWAY FROM ZERO,
       unlike the global picture where the sup of 4 prod b - sum b_i t_i is 0.
       On the core: t_i in [0.00097, 0.99972], median 0.5519
                    b_i in [0.04821, 0.96152], median 0.5084
       So t_i is LARGE exactly where it is needed -- the term that must be spent
       is the one that is big.  Extremal core witness:
         KEY = 5.3379e-04, i = 1,
         b = (5.83896099e-05, 5.50359834e-01, 1.20241202e-01)
         t = (2.44313676e-01, 9.71642260e-04, 1.87342675e-06)
  READING: the b=0 degeneracy that blocks a strict certificate for KEY lives
  ENTIRELY in the easy branch (where the other two terms carry it).  The core --
  the only place detM is genuinely needed -- has a strictly positive margin.
  A SUCCESSOR SHOULD CERTIFY THE TWO BRANCHES SEPARATELY: the easy branch by the
  pair constraints and step 1, the core by a certificate that may be STRICT,
  because its margin is bounded below.  That is why the single global Handelman
  LP kept failing: it was being asked for one certificate covering a degenerate
  branch and a non-degenerate one at once.

F58-12. HANDELMAN ATTEMPTS THIS ROUND, ALL INFEASIBLE — recorded so nobody repeats.
  Generator set {b_i, t_i, A_i, 1-sum t, pair(3), dIM, dM} throughout.
     target sum a <= 1        D=3: 816 products, 769 monomials -- INFEASIBLE
                              D=4: exceeded the time budget
     target KEY               D=2: 136 products, 201 monomials -- INFEASIBLE
                              D=3: 816 products, 1075 monomials -- INFEASIBLE
     target KEY*(b1+b2+b3)    D=2 and D=3 -- INFEASIBLE
  The degeneracy-multiplied form does NOT help, which is itself informative: the
  obstruction is the BRANCH MIXTURE of F58-11, not the vanishing at b=0 alone.

F58-13. THE BRANCH-SEPARATED CERTIFICATE ALSO FAILS — MY OWN F58-11 READING IS
  REFUTED, and this is the round's most useful negative.
  Following F58-11 I certified the CORE branch alone: generators as in F58-12
  plus  negE := 4 b1b2b3 - b2 t2 - b3 t3  (>= 0 defines the core),
  plus the ordering generators b1-b2 >= 0, b1-b3 >= 0 (b1 the max).
     D=2:  190 products -- INFEASIBLE
     D=3: 1330 products -- INFEASIBLE
  So separating the degenerate branch does NOT rescue the Handelman route, even
  though the core has a strictly positive margin (min KEY = 5.34e-4, F58-11).
  A target with a positive margin whose Handelman LP is infeasible at the
  natural degrees means the certificate needs either much higher degree or a
  generator the set does not contain.  DO NOT spend another round on Handelman
  with this generator set at low degree.
  WHAT THIS LEAVES, in order: (i) the Cayley rational corner chart (F58-10c,
  untested) which may change the generator set entirely; (ii) a genuine
  Positivstellensatz with products of dM against itself (dM^2 terms), which
  Handelman's linear-in-generators form cannot express; (iii) a hand proof of
  KEY on the core using dM as an EQUATION near its boundary rather than as an
  inequality generator.

F58-14. ROUND SUMMARY (fork 58).  E1 IS NOT CLOSED and I did not state it.
  LANDED, 14 theorems across two modules, all pins standard, per-module builds
  exit 0, both umbrellas insertions-only:
    Gtz/Wave/BranchTwoDeterminantSum.lean  (commit 4f05761, 9 theorems)
      the determinant sum identity, the linear consequence, and the centre
    Gtz/Wave/CornerDescentRank.lean        (commit 5f8c59c, 5 theorems)
      the kernel resolution and the 3-vs-1 asymmetry, exact
  REFUTED WITH WITNESSES: the linear relaxation (1.0671), every single weighted
  combination (best 1.024 at lam~2), the all-three-pair-tight reading of the
  maximiser (it is the centre, where dM fails), the sufficient condition
  4 b_j b_k <= t_i (fails at a third of the hard branch), and the
  branch-separated Handelman certificate (infeasible at D=2,3).
  THE GAP: KEY of F58-5, one scalar inequality in six variables, measured true
  with the degeneracy and the core both located.

============ ROUND 6 (fork 65) — KEY SURVIVES A FALSE ALARM; detM IN (b,t) ============

F65-1. THE detM CONDITION, WRITTEN IN (b,t) — the form the hand proof needs.
  Substituting x_i = b_i (1-t_i)/t_i into 4 prod x <= 1 + sum x and clearing the
  positive denominator t1t2t3 gives, EXACTLY (sympy-verified both directions):
     detM >= 0   <=>   4 b1b2b3 A1A2A3  <=  t1t2t3 + b1 t2t3 A1 + b2 t1t3 A2
                                             + b3 t1t2 A3        (A_i = 1-t_i)
  This is the (b,t) twin of F58-1's detM = t1t2t3 + sum a_i t_j t_k - 4a1a2a3,
  and it is the form to use when KEY is attacked in (b,t) rather than in x.

F65-2. A FALSE COUNTEREXAMPLE TO KEY, AND WHY IT WAS FALSE — the NINTH thin-tail
  event of this campaign, and the first where the ARTIFACT WAS THE OPTIMISER
  LEAVING THE DOMAIN rather than a sampler failing to reach it.
  SLSQP adversarial descent (6000 restarts) on the FULL system reported
     min KEY = -1.952063e-08  at  b = (0.49999996, 0.49999996, 0.50000004),
                                  t = (0.399132631, 0.600867330, 1.027e-13)
  with both determinant slacks reported at ~-1.5e-15, i.e. "numerically feasible".
  RECHECKED AT 40 DECIMAL DIGITS (mpmath):
     detM  = -3.13885634898e-10     <-- STRICTLY NEGATIVE, INFEASIBLE
     detIM = -3.199999744e-15
     KEY   = -1.94999438667e-08
  The point is OUTSIDE the domain; the "violation" is the optimiser sitting a
  hair beyond the boundary where KEY has already gone negative.  KEY IS NOT
  REFUTED.  Strictly feasible high-precision sampling (5055 accepted points)
  gives min KEY = +5.74976883032e-05.
  RULE FOR THE CAMPAIGN: a constrained optimiser reporting a violation with
  constraint slacks at machine epsilon has proved NOTHING -- recheck the witness
  in extended precision BEFORE believing it.  Feasibility tolerance is not
  feasibility.  (Distinct from the sampler-reachability artifact of fork 60 and
  the chart-coverage artifact of fork 59: here the point was reachable and the
  chart was fine, but the point was not in the set.)

F65-3. THE DEGENERATE RAY, EXACTLY — a whole line of triple zeros.
  At  b = (1/2,1/2,1/2),  t = (r, 1-r, 0)  for ANY r in (0,1):
     detIM = 0,    detM = 0,    KEY = 0     -- all three vanish IDENTICALLY in r.
  (sympy-exact, symbolic in r.)  This is the true structure the numerics were
  hovering around, and it explains F58-4's "supremum 1 approached with t -> 0"
  and fork 47's "exact double root at the centre" as the SAME object seen twice.
  It also explains why every Handelman LP failed: a certificate would have to be
  tight along an entire ray, not at an isolated point.

F65-4. NO FIRST-ORDER DESCENT EXISTS ON THE RAY — the local obstruction, mapped.
  Perturb b_i = 1/2 + p_i e, t = (r - u e, 1-r - v e, q e).  Then EXACTLY:
     detIM_1 = 0                       (vanishes to first order in EVERY direction)
     detIM_2 = 2 (p1p2 + p1p3 + p2p3)  (so the SECOND order gates feasibility)
     detM_1  = [ -2r(1-r)(p1+p2) + (q-u-v) ] / 2
     KEY_1   = [ -2(1-r)p1 - 2r p2 - 2p3 + (q-u-v) ] / 2
  Constraints at first order: p_i + p_j <= 0 (pair), q >= 0 (t3 >= 0),
  q - u - v <= 0 (sum t <= 1), detM_1 >= 0, detIM_2 >= 0.
  EXHAUSTIVE SEARCH over the integer grid p in [-2,2]^3, q in [0,2],
  u,v in [-2,2] at r = 1/2: NO direction satisfies detM_1 >= 0 and KEY_1 < 0.
  So KEY holds to first order along the whole ray, and any counterexample would
  have to be second-order -- consistent with detIM_2 being the active gate.
  Two explicit directions, exact, both with KEY > 0:
     p=(-1,-1,-1), q=u=v=0:  detIM = e^2(6-4e),  detM = e(2e^2-3e+1)/2,
                             KEY = 2e(2e^2-3e+1)      -- all positive at small e
     p=(-1,-1, 1), q=u=v=0:  detIM = e^2(4e-2) < 0    -- INFEASIBLE, correctly
  CAUTION BANKED: my first LP for this direction had a SIGN ERROR in the
  objective and reported a spurious descent at p=(-1,-1,-1); evaluating the
  direction exactly is what caught it.  Evaluate, do not trust the LP sign.

F65-5. DETERMINANT NECESSITY, RECONFIRMED FROM A NEW DIRECTION.
  detM + pair ALONE do NOT imply KEY: adversarial descent finds
     KEY = -3.038081e-02 at b = (0.36037961, 0.36037961, 0.63962039),
                              t = (0.418861, 0.418861, ~0)
  and that witness is EXCLUDED by detIM (= -0.0281 < 0), verified exactly.
  Independent confirmation of F58-2/F58-3: BOTH determinants are needed.
  (Random sampling under detM+pair gave min KEY = +3.06e-04 over 12877 points --
   another thin tail; the descent found the negative region sampling missed.)

F65-6. KEY IS NOT REFUTED — EXACT-ARITHMETIC STATUS AFTER THIS ROUND.
  Every apparent violation this round dissolved under exact arithmetic:
   (a) SLSQP on the full system: min KEY = -1.95e-08, slacks ~-1.5e-15
       -> at 40 digits detM = -3.14e-10 < 0.  INFEASIBLE.  (F65-2)
   (b) SLSQP on the homogenised KEY/(sum b): min = -1.67e-08 at
       b = (0.50000005, 0.49999995, 0.49999995), t = (1e-9, 0.5868, 0.4132),
       detM = -2.2e-15, detIM = -5.3e-15 -> BOTH negative.  INFEASIBLE, and the
       point is the SAME ray of F65-3 approached from a different restart.
   (c) Exact rational grid around the ray (b offsets {-2,-1,0,1}*step for step
       in 1e-2..1e-5, q in {0,1,2}, r = 2/5): NO feasible point with KEY < 0.
   (d) Exact rational sweep, 120000 draws, 4132 STRICTLY feasible (feasibility
       decided in exact arithmetic, never in floating point):
          min KEY = +3.909010e-04.
  STATUS UNCHANGED FROM F58-5: KEY IS MEASURED TRUE, NOT PROVED.  What is NEW is
  that it now survives exact-arithmetic testing and a first-order analysis on its
  degeneracy, so the earlier "supremum exactly 0" is explained by the ray rather
  than by a hidden violation.

F65-7. TWO ROUTES REFUTED THIS ROUND, WITH WITNESSES.
  (a) detM + pair WITHOUT detIM does NOT imply KEY (F65-5): witness above with
      KEY = -3.04e-02, excluded only by detIM.  So no certificate may drop detIM.
  (b) THE DIVIDED FORM of detM does not close it either.  detM >= 0 divided by
      A1A2A3 > 0 reads  4 b1b2b3 <= t1t2t3/(A1A2A3) + sum_i b_i t_jt_k/(A_jA_k),
      so KEY would follow from the sufficient condition
         G := t1t2t3 + sum_i b_i t_j t_k A_i^2 - (sum_i b_i t_i) A1A2A3  <=  0 .
      MEASURED: G FAILS on the feasible set, max G = +1.484464e-01 at
         b = (0.08558506, 0.70900491, 0.0963671),
         t = (0.48634728, 0.00712414, 0.50211166).
      Recorded so nobody re-derives it.

F65-8. WHAT THE SUCCESSOR SHOULD DO, sharpened by the ray.
  The obstruction is now precisely located: KEY is tight on the ENTIRE RAY
  b = (1/2,1/2,1/2), t = (r, 1-r, 0), r free, where detIM = detM = KEY = 0.
  A Positivstellensatz certificate must therefore be tight along a
  one-parameter family, which is exactly what a certificate with only isolated
  tightness cannot do -- the structural reason for every Handelman failure in
  F58-6 and F58-12, now UNDERSTOOD rather than merely observed.
  THE THREE ROUTES, re-ordered by what the ray implies:
   (i)  QUOTIENT THE RAY, not just the b=0 point.  Certify KEY on a transversal
        slice to the ray (e.g. add the normalisation t3 = c > 0, or work in
        coordinates where the ray is a coordinate axis) and handle the ray
        itself by the exact vanishing of F65-3.  This is the natural successor
        to F58-10(a) now that the degeneracy is known to be one-dimensional.
   (ii) The Cayley rational corner chart (F58-10c) -- STILL UNTESTED by me or
        by fork 58.  It may present the ray as a coordinate locus.
   (iii) A genuine Positivstellensatz with dM^2 terms (F58-10 route ii).
  DO NOT re-run: the low-degree Handelman LPs (F58-6, F58-12), the
  branch-separated variant (F58-13), the divided form (F65-7b), or detM+pair
  without detIM (F65-7a).

F65-9. ROUND SUMMARY (fork 65).  E1 IS NOT CLOSED and I do not state it.
  NO NEW LEAN THIS ROUND -- nothing survived that was not already landed, and I
  will not land a measured statement as a theorem.
  ADVANCES: the (b,t) form of detM (F65-1); the exact degenerate RAY and the
  proof that no first-order descent leaves it (F65-3, F65-4); KEY re-established
  under exact arithmetic after a false alarm (F65-2, F65-6); two more routes
  refuted with witnesses (F65-7).
  METHOD RULE ADDED: a constrained optimiser reporting a violation with
  constraint slacks at machine epsilon has proved nothing.  Recheck in extended
  precision before believing it -- feasibility tolerance is not feasibility.

F65-10. THE DEGENERATE RAY IS NOT IN THE IMAGE OF THE CORNER MAP — the round's
  most useful structural finding, and it is an ARGUMENT, not a measurement.
  The ray of F65-3 requires  sum t = 1  exactly, where t = (t1,t2,t3) are the
  THREE OUTSIDE weights.  Parseval fixes the sum of ALL SIX weights at 1
  (WeightedDesign.weight_sum_one), so
       sum t = 1   <=>   the three INSIDE weights sum to zero
                   <=>   every inside weight is zero,
  which WeightedDesign.weight_pos forbids.  Hence NO DESIGN maps to the ray:
  it is a boundary of the (b,t) BOX that the corner map approaches and never
  reaches.  This is the same weight-collapse boundary that fork 61 measured for
  the K0 joint (inf|F| = 0.0026/0.0142/0.0229/0.0305 at weight floors
  0.00/0.01/0.03/0.05) and fork 45 for K1 -- a THIRD arm hitting the identical
  artifact, and here it is settled by Parseval rather than by descent.
  SUPPORTING MEASUREMENT (not the argument): 34964 real corners built with all
  outside excesses positive gave min max_i |b_i - 1/2| = 0.0216 and sum t range
  [0.1000, 0.9900]; points with |b - 1/2| < 0.05 AND sum t > 0.9: ZERO.
  CAVEAT, STATED PLAINLY: I did not verify that the corner image is bounded away
  from the ray UNIFORMLY.  Nelder-Mead targeting of the ray over a 16-parameter
  corner construction TIMED OUT at 2 minutes and is unfinished -- treat the
  quantitative gap as unmeasured.  What IS established is the qualitative fact:
  sum t = 1 is unattainable for a design, so the ray is not in the image.

F65-11. WHAT THIS CHANGES FOR THE CERTIFICATE — the successor's best lead.
  Every Handelman/Positivstellensatz failure recorded in F58-6, F58-12, F58-13
  was on the (b,t) BOX, whose closure CONTAINS the ray, where the target is
  tight and no strict certificate can exist.  But E1 only ever needs KEY on the
  IMAGE OF THE CORNER MAP, which by F65-10 excludes the ray.
  SO THE RIGHT MOVE IS TO ADD THE DESIGN'S OWN CONSTRAINT AND RETRY:
     sum t <= 1 - tau   for the inside weight mass tau > 0,
  or, better, carry the inside weights explicitly as variables so that
  sum(all six) = 1 is an EQUATION of the system rather than an unused fact.
  MEASURED (exact rationals, 7522 strictly feasible points, bucketed by
  eps = 1 - sum t):
     eps ~ 1e-0: min KEY = +2.07e-04     eps ~ 1e-3: min KEY = +1.21e-01
     eps ~ 1e-1: min KEY = +2.83e-02     eps ~ 1e-4: min KEY = +7.45e-02
     eps ~ 1e-2: min KEY = +3.78e-02     eps ~ 1e-5: min KEY = +2.00e-01
  KEY does NOT degrade as eps -> 0 in these samples, i.e. the tight direction is
  NOT simply "small inside mass" -- so a bound of the form KEY >= c*eps is NOT
  what the data suggests, and a successor should not assume one.  The honest
  reading: the ray is approached along a thin direction the sampler does not
  favour, and locating that direction is the next measurement.
  DO NOT re-run anything in the F65-8 do-not-list.

F65-12. SCOPE NOTE ON THE CAYLEY CHART (queue item 2) — why I did not use it.
  The Cayley chart parametrises the INSIDE atoms of a corner
  (V = diag(1,1,mu) Q, Q = (I-K)(I+K)^{-1}), and I confirmed it builds corners
  cleanly: 20000/20000 draws gave N_O positive definite.
  But the E1 variables (b,t) are OUTSIDE-triple data -- the weighted outside
  Gram M, the excesses a_i and the outside weights t_i.  The Cayley chart does
  not chart that domain; it charts the corner that produces it.  So it is the
  right tool for the CELLS lane (corner geometry) and the wrong tool for KEY
  as currently posed.  It becomes relevant only via F65-11, i.e. if the inside
  weights are carried explicitly -- and THEN the Cayley chart is exactly how to
  parametrise the inside atoms rationally.  Recorded so the next fork does not
  spend a round discovering the mismatch.

F65-13. THE SHARPEST STATEMENT OF THE E1 GAP AVAILABLE — where sum a > 1 lives.
  Maximising sum a over the FULL box system {detIM>=0, detM>=0, pair, sum t<1}
  (SLSQP, 5000 restarts) gives
     max sum a = 1.0000000281   at  b = (0.50000006, 0.49999994, 0.49999994),
                                     t = (1e-12, 0.424012227, 0.575987716),
                                     sum t = 0.99999994,
     detM = -2.5e-15,  detIM = -6.7e-15   -- BOTH at machine epsilon, i.e. the
  optimiser is sitting ON the ray of F65-3 and marginally outside the domain,
  the same artifact as F65-2.  Exactly-feasible sampling with the target's own
  hypothesis sum a > 1 imposed found ZERO points in 200000 draws.
  READING, stated carefully because an empty sample is weak evidence:
  the region {sum a > 1} of the box appears to touch the feasible set ONLY at
  the ray, and the ray is not in the image of the corner map (F65-10).  If that
  is exact -- and I did NOT prove it -- then E1 on the box is true for the
  degenerate reason that its hypothesis and its conclusion collide only where no
  design lives.  A successor should test this directly: is
        {detIM >= 0, detM >= 0, pair, sum t <= 1 - tau, sum a >= 1}
  EMPTY for every tau > 0?  That is a cleaner target than KEY, it is a
  Positivstellensatz EMPTINESS question (not a tight inequality), and emptiness
  certificates do not suffer the "cannot be strict" obstruction that killed
  every attempt in F58-6, F58-12, F58-13 and F65-8.
  THIS IS THE ROUTE I WOULD TAKE NEXT.

F65-14. ROUND SUMMARY, FINAL (fork 65).  E1 IS NOT CLOSED; I do not state it,
  and I hand over no gateway.
  NO NEW LEAN.  Nothing this round survived as a theorem, and the one candidate
  that looked like a counterexample was an artifact.  Three notes commits.
  WHAT ADVANCED:
   * the (b,t) form of detM, exact both directions            (F65-1)
   * KEY re-established under exact arithmetic after a false alarm (F65-2, F65-6)
   * the degeneracy identified as a RAY of triple zeros, with no first-order
     descent leaving it                                        (F65-3, F65-4)
   * the ray shown UNREACHABLE by any design, from Parseval    (F65-10)
   * the missing constraint named: the inside weight mass tau_C, which the box
     formulation discards                                      (F65-11)
   * the target reformulated as an EMPTINESS question that avoids the tightness
     obstruction entirely                                      (F65-13)
  REFUTED WITH WITNESSES: detM+pair without detIM (F65-7a); the divided form of
  detM (F65-7b); and the Cayley chart as a route to KEY as posed (F65-12).
  METHOD RULES ADDED: feasibility tolerance is not feasibility -- recheck any
  optimiser-reported violation in extended precision (F65-2); and evaluate a
  candidate descent direction exactly rather than trusting an LP's sign (F65-4).

============ ROUND 7 (fork 66) — THE SLACK FACTORIZATION, AND A 2-SIMPLEX ============

F66-1. THE COORDINATE CHANGE THAT MAKES EVERYTHING DEGREE 3.
  Work in (a,t), NOT (b,t).  With A_i = 1-t_i and a_i = b_i A_i:
     a_i >= 0                                   deg 1
     t_i >= 0,  S := 1 - t1 - t2 - t3 >= 0      deg 1
     p_ij := A_iA_j - a_iA_j - a_jA_i >= 0      deg 2   (the pair constraint)
     dIM  := A1A2A3 - sum a_i A_jA_k + 4a1a2a3  deg 3
     dM   := t1t2t3 + sum a_i t_jt_k - 4a1a2a3  deg 3
     target 1 - sum a                            deg 1
  In (b,t) the determinants are degree SIX (a_i = b_i A_i is degree 2), which is
  why every prior Handelman run was expensive.  In (a,t) degree-4 Handelman is
  1820 products over 2612 monomials -- small.  Recorded because it is reusable.

F66-2. THE DEGENERACY IS A 2-SIMPLEX, NOT A RAY -- CORRECTION TO F65-3.
  F65-3 recorded the triple-zero locus as the one-parameter ray
  b = (1/2,1/2,1/2), t = (r, 1-r, 0).  It is bigger: EVERY point with
     b = (1/2,1/2,1/2)   and   t1+t2+t3 = 1     (t anywhere on the 2-simplex)
  has dIM = 0, dM = 0, all three pair minors 0, S = 0, sum a = 1, AND
  sum a_i t_i - e2 = 0.  Checked in exact rationals at six random weight
  vectors (scratchpad/f66/degen.py); the F65-3 ray is just the edge t3 = 0.
  CONSEQUENCE: a certificate for the target must vanish on a TWO-dimensional
  set.  That is a sharper obstruction than F65-3 stated, and it is the honest
  reason the Handelman LPs fail.

F66-3. THE SLACK FACTORIZATION (LANDED, one ring).  Substituting
  1 - t_j - t_k = S + t_i  and  A1A2A3 + t1t2t3 = S + e2  into F58-1 gives
     dIM + dM = (S + e2) - ( S*(sum a) + sum a_i t_i )
  hence, with e2 = t1t2+t1t3+t2t3,
     **  S * (sum a - 1)  =  e2 - sum a_i t_i - (dIM + dM)  **
  Verified at 4000 random rational points with ZERO mismatch BEFORE proving
  (scratchpad/f66/lin.py); it is a polynomial identity, no hypothesis.
  LANDED as Gtz.branchTwo_slack_factorization, Gtz/Wave/CornerEmptinessSlack.lean.

F66-4. WHAT F66-3 SETTLES, AND WHAT IT DOES NOT.
  (a) The target times the slack is EXPLICIT.  On S > 0,
        sum a <= 1   <==>   e2 <= sum a_i t_i + dIM + dM
      (Gtz.branchTwo_target_iff_defect).  The two determinants enter ONLY as a
      sum, so this is the exact strength of the summed conditions -- F58-2's
      1.0671 and F58-3's whole one-parameter family are now explained
      structurally, not just measured.
  (b) Dropping both determinants leaves  S*(sum a - 1) <= e2 - sum a_i t_i
      (Gtz.branchTwo_slack_bound), and the right side IS genuinely positive on
      the feasible set: at a = 0, t = (1/3,1/3,1/3) it is 1/3 while every
      hypothesis holds (dIM = 8/27, dM = 1/27, all p_ij >= 0, S = 0).
  (c) HONEST LIMIT: F66-3 is an exact reformulation, so it does NOT by itself
      advance the proof.  Any attempt to use it circularly returns the target.
      Its value is diagnostic and as a Lean stepping stone.

F66-5. THE DEGREE-2 REDUCTION, TESTED AND REFUTED WITH A WITNESS.
  F66-3 suggests: if the OTHER constraints forced sum a_i t_i >= e2, then with
  S > 0 the target would follow immediately.  TESTED over
  {pair, S >= 0, sum a >= 1, b,t in [0,1]} by SLSQP, 4000 restarts:
     MIN of (sum a_i t_i - e2) = -0.25   at   b = (1,0,0),  t = (0, 1/2, 1/2)
  and that witness is FULLY FEASIBLE for the whole system:
     p = (0,0,0),  S = 0,  sum a = 1,  dIM = 0,  dM = 1/4 >= 0.
  So the degree-2 reduction is FALSE, and note the witness is a degeneracy
  point OUTSIDE the F66-2 simplex (b is a vertex of the pair polytope, not the
  centre).  The full degenerate set is therefore larger than F66-2 alone.
  DO NOT re-run this reduction.

F66-6. HANDELMAN IN (a,t) -- STATUS, INCLUDING ONE INCONCLUSIVE RUN.
  Target 1 - sum a, generators {a_i, t_i, S, p_ij, dIM, dM} (12 of them):
     D=2:   91 products,  165 monomials -- INFEASIBLE
     D=3:  455 products,  769 monomials -- INFEASIBLE
     D=4: 1820 products, 2612 monomials -- HiGHS status 4, NUMERICALLY
          INCONCLUSIVE after 278 s.  NOT a refutation.  Recorded as such.
  F58-6 reported D=4 as "exceeds the time budget" in (b,t); in (a,t) it RUNS
  but does not decide.  A successor wanting D=4 should re-run it with exact
  rational arithmetic or a rescaled LP, not trust the float result either way.

F66-7. ROUND SUMMARY (fork 66).  E1 IS NOT CLOSED; I do not state it and I hand
  over no gateway.
  LANDED: Gtz/Wave/CornerEmptinessSlack.lean, 10 theorems, all pins
  [propext, Classical.choice, Quot.sound], per-module build exit 0.
  ADVANCED: the (a,t) coordinates (F66-1); the degeneracy corrected upward to a
  2-simplex (F66-2); the slack factorization, exact and landed (F66-3); the
  exact strength of the summed conditions (F66-4).
  REFUTED WITH WITNESSES: the degree-2 reduction sum a_i t_i >= e2 (F66-5).
  INCONCLUSIVE, flagged: Handelman D=4 in (a,t) (F66-6).
  WHAT I WOULD DO NEXT: the degenerate set is now known to contain both the
  F66-2 centre simplex and the F66-5 vertex point, so it is a union of faces.
  Enumerating ALL of it, then certifying the target on each face separately, is
  the route the two obstructions point at -- a face-by-face argument, not a
  single certificate.

F66-8. **A CAVEAT ON THE (a,t) BOX ITSELF, FOUND BY TESTING IT AT REAL CORNERS.**
  I tried to read F66-3 at the design level.  The READING is right:
     S = tau_C (the inside weight mass)  and  sum a - 1 = 1 - s  (s the inside
     share), both confirmed to 1e-9 at 1750 genuine corners.
  But the IDENTITY did not reproduce there: max |err| = 1.33, not 0.  The cause
  is not the identity -- that is a polynomial fact -- it is that
  Gtz.branchDetComplement and Gtz.branchDetOutside are DEFINITIONS, polynomials
  in (a,t) alone, and they are NOT det(1-M) and det(M) at a general corner.
  MEASURED (scratchpad/f66/proxy.py).  Build M with diagonal t_i + a_i and free
  off-diagonals m_ij, so K = M - diag(t) has K_ii = a_i, K_ij = m_ij and pair
  minor q_ij = a_i a_j - m_ij^2.  Then, max error over 3000 draws each:
     random off-diagonals            dM - det M = 2.51e-01,  dIM = 5.75e-01
     pair-tight (q_ij=0), sigma=+1   dM - det M = 8.80e-02,  dIM = 8.80e-02
     pair-tight (q_ij=0), sigma=-1   dM - det M = 4.34e-17,  dIM = 3.33e-16
  So the two definitions coincide with the true determinants EXACTLY on the
  slice { all three pair minors zero, Bargmann sign sigma = -1 } and nowhere
  else.  This is consistent with F47-8, whose exact formula
     det(K_D - I) = -2 prod x + 2 sigma sqrt(prod(x_i x_j + r_ij)) - sum x_i r_jk
  reduces at r = 0, sigma = -1 to the -4 prod a term the definitions carry.
  WHAT THIS DOES AND DOES NOT MEAN.  It does NOT invalidate anything landed:
  every theorem in BranchTwoDeterminantSum, BranchTwoReduction, CornerDescentRank
  and CornerEmptinessSlack is an unconditional polynomial identity or an
  implication from stated hypotheses, and none of them claims the definitions
  are determinants.  What it means is that the (a,t) box is a SIX-DIMENSIONAL
  SLICE of the true problem -- the boundary r = 0 of the inadmissible region,
  at one Bargmann sign -- and the true problem carries r_ij >= 0 free and both
  signs.  Whether the extremum of sum a over the true inadmissible region is
  attained on that slice is a step I did NOT find stated anywhere in this lane's
  notes, and it is not implied by F58-4 (whose "no pair tight" refers to
  b_i + b_j = 1, a DIFFERENT condition from q_ij = 0).
  RECOMMENDATION: before another certificate round, settle whether sup sum a
  over {q_ij <= 0, both signs} equals sup over the r = 0, sigma = -1 slice.  If
  it does, say so and cite it; if it does not, the box target is the wrong one
  and every Handelman failure in F58-6, F58-12, F58-13, F65-8 and F66-6 was
  aimed at a proxy.  This is cheap to test and it gates the whole lane.

============ ROUND 8 (fork 67) — THE TIGHT FAMILY, AND NO CERTIFICATE EXISTS ============

F67-1. **THE TIGHT FAMILY OF THE BOX, EXACT AND CLOSED FORM.**  The box system
  carries a TWO-PARAMETER family of points at which the target holds with
  EQUALITY while the weight slack stays strictly positive:

      t = (0, t2, t3)      a = ( 1 - (t2+t3)/2 ,  t3/2 ,  t2/2 )

  Verified SYMBOLICALLY (sympy, scratchpad/f67/fam1.py), exactly zero in every
  slot, for all t2, t3:
      dIM = 0            dM = 0            sum a - 1 = 0
      p12 = t2*(1-t2-t3)/2          p13 = t3*(1-t2-t3)/2
      p23 = (1-t2-t3)*(2-t2-t3)/2
  so with 0 < t2, 0 < t3, t2+t3 < 1 ALL THREE PAIR SLACKS ARE STRICTLY
  POSITIVE — the family is DEEP INSIDE the pair polytope, not on its boundary,
  and S = 1 - t2 - t3 is free in (0,1).
  Note b = (1-(t2+t3)/2, t3/(2(1-t2)), t2/(2(1-t3))) — the CROSS pairing
  a2 = t3/2, a3 = t2/2 is the shape, and it is a DOUBLE root: at t1 = 0 the
  system {dM=0, sum a=1} reduces to (2*a3 - t2)^2 = 0.

F67-2. **TWO RECORDED CONJECTURES DIE.  Both were reachability artifacts.**
  (a) `1 - sum a >= c*S` is FALSE for every c > 0 (the family has 1-sum a = 0
      at every S in (0,1)).  The recorded measurement `min (1-suma)/S = 0.0248`
      came from a sampler that could not reach t1 = 0.
  (b) F65-13's emptiness question — is
      {dIM>=0, dM>=0, pair, sum t <= 1-tau, sum a >= 1} empty for every tau>0 —
      is answered **NO**.  The family sits inside it for every tau in (0,1).
      DO NOT re-run that question as posed.
  Also corrected: the recorded `max sum a = 0.988188` is wrong; the supremum is
  exactly 1 and it is ATTAINED on the family.

F67-3. **THE FAMILY LIVES ONLY ON t1*t2*t3 = 0.**  The tight system
  {sum a = 1, dIM = 0, dM = 0} is three equations; (1) and (2) are linear in a
  and (3) is a cubic on that line, so up to three roots per weight vector.
  Solved EXACTLY (sympy, scratchpad/f67/cubic.py) at four rational weight
  vectors with all t_i > 0 — t = (1/10,1/5,3/10), (1/4,1/4,1/4), (1/6,1/3,1/4),
  (1/20,2/5,1/4).  Every time: two complex roots and one real root with
  NEGATIVE components (e.g. a = (-0.00647, 1.91294, -0.90647)).  ZERO feasible
  tight points with all weights positive.

F67-4. **THE HANDELMAN OBSTRUCTION — NO CERTIFICATE EXISTS AT ANY DEGREE.**
  This is the round's main result and it is LANDED.  Two exact rational
  witnesses, sharing ONE weight vector t = (0, 3/10, 1/4):

    W1  a = (29/40, 1/8, 3/20)     b = (29/40, 5/28, 1/5)
        sum a = 1 exactly,  1 - sum a = 0
        dIM = 0, dM = 0, S = 9/20
        pairs = (27/400, 9/160, 261/800)      all > 0
        amgm  = (27/80, 63/200, 9/20)         all > 0
        caps  = (11/40, 23/40, 3/5)           all > 0
        A = (1, 7/10, 3/4),  e2 = 3/40 = sum a_i t_i
        ==> the ONLY vanishing generators are { t1, dIM, dM }

    W2  a = (0, 7/20, 3/8)         b = (0, 1/2, 1/2)
        sum a = 29/40,  1 - sum a = 11/40 > 0
        dIM = 0, dM = 0, t1 = 0, S = 9/20
        pairs = (7/20, 3/8, 0)    amgm = (7/10, 3/4, 0)   caps = (1, 7/20, 3/8)

  ARGUMENT.  Write the certificate as sum_alpha lambda_alpha * (monomial in the
  generators), lambda >= 0.  At W1 every generator is >= 0 and the target is 0,
  so every monomial with a positive coefficient is 0 at W1, hence carries a
  generator that vanishes at W1, hence one of {t1, dIM, dM}.  At W2 all three of
  those are 0, so the whole certificate is 0 there — but the target is 11/40.
  Contradiction.  **NO DEGREE BOUND APPEARS**, so no amount of degree helps.
  Verified exactly in scratchpad/f67/witness.py.

F67-5. LP RUNS, ALL WITH AN ENRICHED GENERATOR SET NO EARLIER ROUND USED.
  Generators {a_i, t_i, A_i, c_i = A_i - a_i, S, p_ij, m_ij = A_iA_j - 4a_ia_j,
  dIM, dM}.  The m_ij are the arithmetic-mean bounds 4 b_i b_j <= 1: they follow
  from the pair constraint through the SQUARE (b_i - b_j)^2 >= 0 and are
  therefore NOT products of the other generators.  Adding them is a genuine
  strengthening, and it still fails:
     target 1 - sum a :  D=2 infeasible, D=3 infeasible, D=4 infeasible
  **This CORRECTS F66-6's "D=4 numerically inconclusive".**  In the enriched set
  the D=4 LP is 3062 columns by 210 rows and HiGHS returns a clean INFEASIBLE.
  D=3 infeasibility is now EXPLAINED, not just observed: F67-1 forces every
  monomial to carry dIM, dM, or all three weights, and F66-2's centre simplex
  forces a second factor, so degree 3 cannot reach.

F67-6. THE WEIGHT-MULTIPLIED TARGET AND THE WEIGHT FLOOR — BOTH STILL FAIL.
  F67-4's obstruction does NOT block t1*t2*t3*(1 - sum a) (that target vanishes
  at both witnesses), so it was worth trying.  It is infeasible at D=4 and D=5
  (12444 products, 462 monomials).
  Adding a weight floor generator t_i - tau with tau = 1/10 — the repair F67-4
  points at, since both witnesses have a zero weight and no design does —
  is infeasible at D=2, D=3 and D=4 (5818 products).
  READING: with a floor the feasible set is COMPACT and the target is STRICTLY
  POSITIVE on it, so no two-point obstruction of the F67-4 kind can exist there.
  The failure is therefore one of COMPLETENESS: the constraint set is not a
  polytope (dIM and dM are cubic), so Handelman's theorem does not apply and
  products of generators are simply not a complete dictionary.  **The lane's
  method has been wrong since F58: it needs SQUARE multipliers (Positivstellensatz
  /SOS), or a hand proof, not an LP over products.**

F67-7. NUMERICS, FOR THE RECORD.  Julia, 100 threads, 3,000,000 refined samples
  of the box system with local ascent (scratchpad/f67/hunt.jl): sum a NEVER
  exceeded 1; best 0.9999484344883168, and the maximiser always drove min(t) to
  zero (min t = 3.65e-6 at the record).  Continuation of the family off the
  boundary (scratchpad/f67/impossible.py), keeping dIM = dM = 0 while raising
  t1 from 0 at t2 = 0.30, t3 = 0.25, stays FEASIBLE throughout and gives
     t1 = 1e-4  -> 1 - sum a = 6.207e-05
     t1 = 1e-3  -> 6.204e-04
     t1 = 1e-2  -> 6.173e-03
     t1 = 0.05  -> 3.014e-02
     t1 = 0.10  -> 5.814e-02
     t1 = 0.20  -> 1.047e-01
  i.e. 1 - sum a is LINEAR in t1 near the family with slope about 0.62 there.

F67-8. LANDED.  Gtz/Wave/GatewayCertificateObstruction.lean, commit e2b34e6,
  22 theorems, every pin [propext, Classical.choice, Quot.sound], per-module
  build exit 0, both umbrellas wired insertions-only.
  Contents: a REUSABLE two-point obstruction lemma for certificates of
  Handelman type (Gtz.certValue_eq_zero_of_obstruction — stated for an arbitrary
  generator count, usable by ANY lane before it spends a round on an LP); the
  family with all six identities; the pair-slack positivity; the refutation of
  the slack bound; the two witnesses with every generator value; and
  Gtz.gateway_no_handelman_certificate.

F67-9. ROUND SUMMARY (fork 67).  E1 IS NOT CLOSED and I hand over no gateway.
  REFUTED WITH EXACT WITNESSES: the `1 - sum a >= c*S` conjecture (F67-2a); the
  F65-13 emptiness question as posed (F67-2b); the recorded max sum a and the
  recorded min ratio (F67-2); F66-6's "inconclusive" D=4 reading (F67-5).
  PROVED AND LANDED: the tight family, and that NO Handelman-type certificate
  exists at any degree (F67-4).
  WHAT A SUCCESSOR SHOULD DO.  Stop running LPs over products of generators —
  F67-6 shows the dictionary is incomplete for this set, and F67-4 shows the
  unfloored target is hopeless outright.  Either (i) prove sum a <= 1 by hand
  using that the tight set is exactly the t1*t2*t3 = 0 boundary (F67-3), or
  (ii) drop the box: F66-8 and the parent's Gtz.det_sum_le_box_add_pairMinors
  make the box the EXTREMAL case of the true problem, so the true problem may
  be easier, not harder, once the off-diagonals r_ij are carried.
